# sshweb 项目交接文档

> 项目根目录:`C:\Users\root\project\cat\sshweb`(Windows) /
> `/mnt/c/Users/root/project/cat/sshweb`(WSL)

## 一、项目是什么

基于原 [sshx](https://github.com/ekzhang/sshx)(协作共享终端)大幅裁剪改造后的 **Web SSH 终端**:

- 浏览器打开即得 shell,支持 **多标签页终端**(可拖拽排序)
- 支持连接 **远程 SSH 服务器**(用户名/密码或密钥认证)
- 内置 **文件管理**(SFTP/本地),支持文件 **编辑器**(语法高亮 / 编码转换 / 行 diff)
- 顶部显示 **服务器状态**(CPU / 内存 / 上传 / 下载速率 / 时间)
- **设置**里可改主题 / 回滚行数 / **字号(8–32,默认 14)**;字号是几何改动,改完会重算行列并同步给远端(坑 94)

原项目的协作、端到端加密、gRPC、mesh 分布式等能力已全部移除。

---

## 二、架构总览

### 运行流程

```
浏览器 (SvelteKit SPA)
  │  WebSocket /api/s/{name}
  ▼
sshweb-server (Rust, axum)
  ├─ 本地终端: fork + openpty + exec shell
  ├─ 远程终端: russh SSH 客户端(密码/密钥认证) + PTY 通道
  ├─ 文件:     本地 = std::fs;远程 = openssh-sftp-client(经 russh channel 的 SFTP 子系统)
  └─ 状态:     /api/stats 轮询返回 CPU/内存/网速/时间
```

- **WS attach 到按 key 复用的 `Session`**:路径 `{name}` 是稳定的会话 key(前端存 sessionStorage
  `sshweb-session-key`),刷新/断网重连同 key 回到同一会话(输出环形缓冲回放);无连接时按 `--session-ttl`(默认 1800s)
  由 sweeper 回收。shell 有 `Sid`(u32);标签顺序服务端维护(`ReorderShells`)。
- 消息编码:CBOR(`ciborium`),二进制 WebSocket。

### 目录结构

```
crates/
  sshweb-core/            # Sid / IdCounter / rand_alphanumeric
  sshweb-server/src/
    main.rs lib.rs listen.rs state.rs utils.rs     # CLI / 优雅关闭 / sweeper / Shutdown
    session.rs + session/sftp_ops.rs               # Session(按 key 复用)+ SFTP 编排 run_*
    runner.rs                                      # shell_task(本地 PTY)/ ssh_task(远程 SSH + su 检测 + 编码)
    ssh.rs                                         # SSH 直连/代理/跳板/mac/密钥 + ChannelEvent 分类
    proxy.rs                                       # SOCKS5 隧道(每服务器一端口)+ 本机直连代理(共用注册表,坑 83)
    sftp/                                          # pool(锁外+超时+探测缓存,openssh-sftp-client)/ remote / local / zip / reader
    stats.rs config.rs terminal.rs                 # /proc 统计 / 加密配置+认证 / PTY 驱动(unix)
    web.rs + web/embed.rs                          # 路由+嵌入前端 / auth / keys / download / proxies / socket / protocol
src/  (SvelteKit SPA)
  routes/+page.svelte                              # 根路径渲染 <Session>
  lib/
    Session.svelte                                 # 主界面:导航栏 + 终端/编辑器标签 + 状态条
    ui/Dialog.svelte ui/Portal.svelte              # 自绘弹窗:z-index 分层 + Portal 到 body(2026-09 替 headlessui)
    icons.ts                                      # @lucide/svelte 图标垫片(保留 XxxIcon 旧名;原 lucide-svelte 已 deprecated)
    protocol.ts srocket.ts api.ts auth.ts          # WS 类型 / cbor-x 封装 / request / 认证状态
    connections.ts proxies.ts keys.ts legacy.ts    # 服务器 / SOCKS5 / 密钥 store,旧数据迁移
    clipboard.ts toast.ts storage.ts upload.ts
    sftpView.ts encoding.ts constants.ts path.ts format.ts fileicons.ts
    i18n/ session/ ui/ editor/ file/               # 字典 / runtime·editors·stats / 组件 / 语言·diff / 下载
    ui/(dnd.ts shortcuts.ts dragResize.ts)         # HTML5 DnD / 键盘 action / 拖拽调宽(均在 ui/ 下)
```

---

## 三、编译与运行 (WSL Debian)

### 构建

```bash
cd /mnt/c/Users/root/project/cat/sshweb
npm install && npm run build          # 前端(生成 build/,构建时嵌入二进制)
cargo build --release --bin sshweb      # 产出单个可执行文件 target/release/sshweb (动态 glibc)
```

**单二进制交付**:前端 `build/` 经 `rust-embed` 编译进二进制(`web/embed.rs::Assets`),运行时**不依赖磁盘 build/**,
静态资源(含 gzip/br 预压缩变体)从内存提供。二进制名 **`sshweb`**(`Cargo.toml [[bin]] name = "sshweb"`)。

**静态可移植交付(推荐,部署到旧 glibc 机器)**:

```bash
rustup target add x86_64-unknown-linux-musl
sudo apt-get install -y musl-tools          # 提供 musl-gcc(ring/汇编需要)
cargo build --release --target x86_64-unknown-linux-musl --bin sshweb
# 产出 target/x86_64-unknown-linux-musl/release/sshweb —— 全静态,无任何 glibc 依赖
```

动态版 `target/release/sshweb` 需要目标机 **glibc ≥ 编译机**(WSL 是 2.41);部署到旧系统(如缺
`GLIBC_2.29/2.30/2.32…`)会报 `version 'GLIBC_2.29' not found`。静态版 `ldd` 显示 `statically linked`,
`strings | grep GLIBC_` 为 0,**可在任何 glibc≥2.0 / musl 环境运行**。部署 201 等旧机器用静态版。

仅按 Linux/WSL 构建部署。Windows 原生编译会因
`tokio::signal::unix`/`terminal.rs` 的 `compile_error!` 失败;勿为 Windows 改动 Linux 信号逻辑。

### 发布流程 (tag 触发 CI)

`.github/workflows/ci.yml` 在 push 到 **master** 或推 **`v*` tag** 时跑:前端 `check`/`build` → 固定日期的 nightly
`cargo fmt --check` → `cargo test --workspace`;**tag 事件**额外构建 musl 静态二进制并建 GitHub Release。
发布步骤:dev 合到 master → 推 master → **打注解 tag 并推**。

```bash
git checkout master && git merge --ff-only dev     # dev 是 master 的快进后继
git tag -a v0.5.0 -F /tmp/notes.md                 # 注解内容 = 发布说明
git push origin master && git push origin v0.5.0
```

**发布说明写进 tag 注解**——CI 用 `git tag -l --format='%(contents)'` 取出它,再拼一行 compare 链接当 release body
(取之前先按名 `git fetch` 一次 tag:actions/checkout 留下的是**轻量** tag 引用,不取注解对象时 `%(contents)` 会退化成
提交信息——v0.5.0 第一次发布就把提交信息当成了发布说明)。body 由 `gh release edit --notes-file` 显式落一次:
softprops 只负责建 release + 传产物,**同名 tag 重推时它不刷新 body**。轻量 tag 没有注解,发布页就只有那行 compare 链接。
发版前把 `Cargo.toml` 的 `version`(workspace + `sshweb-core` 依赖两处)与 `vite.config.ts` 的 `__APP_VERSION__`
(设置页「关于」显示用)同步成 tag 的版本号。

**改 workflow 后用 `actionlint` 自查**(单文件二进制,`actionlint .github/workflows/ci.yml`):PyYAML 能解析的
YAML 未必是 GitHub 认的 workflow —— 踩过「一个 step 里重复 `env:` 键」,GitHub 直接判文件非法:run 立刻失败、
**0 秒且没有任何 job**(页面上不写原因,只显示 0 jobs)。

### 运行

```bash
./target/release/sshweb                                  # 默认 [::] 双栈 IPv4+IPv6,端口 2223
./target/release/sshweb -p 2223                          # 指定端口(默认 2223)
./target/release/sshweb --listen 0.0.0.0 -p 2223         # 仅 IPv4(0.0.0.0)
./target/release/sshweb -d                                # 后台启动(默认端口 2223,日志 ./sshweb.log)
./target/release/sshweb -d --log /tmp/sshweb/server.log    # 后台启动,指定日志文件
./target/release/sshweb --tls-cert server.crt --tls-key server.key -p 8443   # HTTPS
# 说明:首次启动会打印一次性「安装密钥」到日志;用它在浏览器登录后强制设置访问密码。
#      HTTPS 时 Cookie 加 Secure + HSTS;非回环地址明文监听会打印安全警告。
# 说明: -d/--detach 后台运行 = fork + setsid 脱离终端,stdin=/dev/null,stdout+stderr 追加到
#      --log(默认 ./sshweb.log)。父进程立即退出,shell 提示符立刻返回。仅对服务器运行有效,
#      与子命令(export/keys/...)不冲突(子命令恒前台)。--log 仅在有 -d 时可用。
# 开发模式: cargo run --bin sshweb -- --listen 127.0.0.1 -p 2223
#          + npm run dev (vite 热更新,代理到 [::1]:2223)
# 配置备份/恢复与密钥(CLI,不经 HTTP):
./target/release/sshweb version                            # 版本号(等价 -V)
./target/release/sshweb status                             # 配置状态(路径/是否已设密码/计数,无需密码)
./target/release/sshweb export -o backup.enc             # 导出完整加密配置
./target/release/sshweb import backup.enc                # 导入并替换
echo 密码 | ./target/release/sshweb keys                 # 列出已存密钥(交互输密码;管道可脚本化)
./target/release/sshweb reset-password                   # 重置访问密码(保留全部配置,重新加密)
echo 密码 | ./target/release/sshweb servers              # 列出已配置服务器(不含密码)
# 说明: keys / servers / reset-password 需访问密码(交互或管道); status 无需密码
# 但计数反映「当前进程是否已解锁」,未解锁进程恒为 0(CLI 一次性进程即未解锁)。
```

> `--listen` 默认 `::`(Linux 双栈 IPv4+IPv6);指定 `0.0.0.0` 才仅 IPv4。`-p`/`--port` 默认 2223。
> 未传 `--shell` 时本地终端默认 shell = 启动环境的 `$SHELL`,未设置则 `/bin/bash`(`state.rs::default_shell`)。早期硬编码
> `/bin/sh` 会让不带 `--shell` 的部署出现 `sh-5.0#` 裸提示符。远程 shell 无 cwd 时走 sshd 登录 shell(`request_shell`),
> 带 cwd(「在目录中启动 SSH」/启动目录)时走远端 `exec "${SHELL:-/bin/sh}" -l`(runner.rs,**`-l` 保证与前者同为登录
> shell**,见坑 76),与本地默认无关。

### 验证/检查

```bash
cargo +nightly fmt --check && cargo check --workspace && cargo test --workspace
cargo build --workspace
npm run check && npx prettier --check --end-of-line auto . && npm run build
```

> **Rust 门禁在 WSL 里跑**:Windows 侧 `cargo test --workspace` 会在**带 C 源码的依赖**上失败(`cc-rs` exit 1,
> 与改动无关;`target/` 两边共用,`target/debug` 里那份是 WSL 建的)。**卡点会随依赖变化而换人**——2026-09-18
> 依赖审计把 `aws-lc-sys` 移出构建图后(坑 88),Windows 侧**仍然**编译不过,这次卡在 `ring` 的 C 代码;根因是
> `C:\msys64\mingw64\bin\gcc.exe` **本身坏掉**了(`gcc.exe -E` / `-c` 对一个最小探针都 exit 1 且 stdout/stderr 全空,
> 任何工作目录都一样),`cc-rs` 连 compiler family 都探测不出。**只要构建图里还有需要 C 编译器的 crate,Windows 侧就
> 跑不了门禁**,不是某个特定依赖的问题;要真在 Windows 侧跑得先修/换 gcc.exe(或改用 MSVC 工具链),那是另一件事。
> 前端门禁在 Windows 侧跑。
> **`npm run lint` 恒不过**:prettier3 默认按 `lf` 判而工作区几乎全是 CRLF(全仓误报),故上面用 `--end-of-line auto`
> 判真实格式(修用 `--write --end-of-line auto <文件>`,别 `--write .`);它现在仍报 4 个文件的**真实**漂移
> (`src/lib/ui/Settings.svelte`、`src/lib/upload.ts`、`src/routes/+error.svelte`、`src/routes/+page.svelte`,自
> 2026-09-15 那批提交起就没跑过 prettier)——要清就单独一次格式化提交。`eslint` 单独跑是干净的(exit 0),
> `npm run check`(svelte-check)也是绿的。

- 首次访问 `GET /api/auth/status`:`setup`(是否已设访问密码)、`authenticated`、`pendingChange`(用安装密钥登录后
  待强制改密)。**首次启动**服务端打印一次性「安装密钥」到日志;未设密码时 `/api/auth/login` 只接受该密钥,登录后
  `pendingChange=true`,只能 `POST /api/auth/password` 设置访问密码(旧 `/api/auth/setup` 已删除)。已设置后显示
  登录。成功经 `HttpOnly; SameSite=Lax` Cookie(`sshweb_auth`)建立会话,有效期 30 天;**HTTPS 部署时 Cookie 加 `Secure`
  并返回 HSTS**。**服务重启清空内存登录会话**,旧页面 WS 收 401 自动重连,由 `AuthGate` 自动回登录页。
- **登录/改密限流**:`/api/auth/login`、`/api/auth/password` 按来源 IP 滑动窗口限流(10 次/15 分钟),超限 429。
- WebSocket、`/api/stats`、HTTP Range 下载、配置读写均需认证 Cookie;静态资源公开,未认证不渲染终端/配置。
  (配置备份/恢复在 CLI 层,不经 HTTP,见下。)
- **配置存储方式**:整个 `ServerSettings`(servers + keys + host_keys,含 SSH 密码与私钥)经 Argon2 派生 AES-256-GCM
  密钥加密后,以 JSON 形式(`EncryptedFile { version, salt, nonce, ciphertext }`)原子写入 `config.enc`(0600)。
  **非明文 json/yml,非数据库**;解密后的配置仅内存。旧 localStorage `sshx-servers-store` 一次性迁移在 `legacy.ts`。
- 配置 API:`GET/PUT /api/config`(明文 servers,keys 仅公开部分;`PUT` 的 `local` 段**缺省 = 不改**本机设置,
  见坑 81)、`POST /api/config/import`(旧迁移)。
  密钥 API:`GET/POST /api/keys`、`PUT/DELETE /api/keys/{id}`(重命名/删除)、`POST /api/keys/install`;连接测试:`POST /api/test-connection`;
  设置页含「SSH 密钥」管理(新增/重命名/删除,`Settings.svelte`)。SOCKS5 隧道/代理:`GET/POST /api/proxies`、`DELETE /api/proxies/{key}`,
  以及本机直连代理的专用端点 `POST /api/proxies/local`(body `{port?,username?,password?}`,三个字段全可缺省;键固定 `"local"`,
  见坑 83)/`DELETE /api/proxies/local`(停止本机直连代理;DELETE 必须另外挂在这条**静态**路径上,参数路由
  `/api/proxies/{key}` 兜不住它,见坑 86)。
  主机密钥:`POST /api/host-keys/forget`(body `{target:"user@host:port"}`,删该目标记录的 TOFU 指纹 → 204;无记录 404;
  空 target 400;未认证 401)。**唯一**的清除入口,由「重新信任」弹窗调用(坑 80)。
- **`error` 响应的可选结构化附加字段**:失败响应体为 `{error: string}`;主机密钥不符时**额外**带
  `hostKeyChanged:{target,expected,actual}`(见坑 80)。前端 `api.ts::ApiError` 保留整个响应体(`err.body`),供
  `hostkey.ts` 取用;不带该字段的响应行为不变。

#### 配置备份 / 恢复 (CLI 完整迁移)

在**后端可执行文件**上提供,不经过 HTTP:`sshweb export` / `sshweb import`。

- **`sshweb export [-o 文件]`** → 导出整个 `config.enc`(加密文件 JSON,含全部服务器密码 + 密钥私钥,私钥在密文内,
  不暴露明文,坑 36)。不带 `-o` 输出到 stdout(可 `> backup.enc`)。
- **`sshweb [--config 路径] import 备份文件`** → 校验并原子替换 `config.enc`,清空解锁状态(下次登录用备份的访问密码)。
- 跨机迁移:旧机 `export -o backup.enc` → 拷到新机 → 新机 `import backup.enc` → 启动服务 → 用**原访问密码**登录。
  实测验证配置与密钥完整恢复。

---

## 四、协议 (WsServer / WsClient,CBOR)

**Server→Client**:`hello(string)` / `shells([[sid,{rows,cols}],...])` /
`chunks([sid, bytes[]])` / `sftpList([sid, path, entry[], truncated])` /
`sftpOk([sid, path])` / `sftpWriteOk([sid, path, offset])`(分块上传 ack,回带已写偏移,用于去重/重试)/
`sftpUploadOk([sid, path, size])`(上传**完成且服务端 stat 校验通过**,唯一代表成功;不符时删半成品并回 `error`)/
`sftpData([sid, path, bytes])`(仅编辑器读取)/
`sftpShell(sid)` / `sftpOpenResult([sid, cwd, user, notice|null])` /
`shellsMeta([[sid, label],...])` / `shellsConfig([[sid, serverConfig],...])` /
`headlessShells([[sid, serverConfig],...])` / `activeShell(sid)` /
`sftpCopyProgress([sid, from, bytes])`(远程复制节流进度)/
`pwd([sid, path])` / `error(string)`

**Client→Server**:`create([x,y,serverConfig,cwd,label])` /
`sftpConnect(serverConfig)` / `sftpOpen(sid)` / `setActive(sid)` /
`reorderShells([sid,...])` / `close(sid)` / `resize([sid,{rows,cols}])` /
`data([sid,bytes])` / `sftpList([sid,path])` / `sftpRead` /
`sftpWrite([sid,path,bytes])` / `sftpWriteAt([sid,path,offset,bytes])` /
`sftpUploadDone([sid,path,total])` / `sftpUploadAbort([sid,path])` /
`sftpMkdir` / `sftpRemove([sid,path,isDir])` / `sftpRename([sid,from,to])` /
`sftpCopy([sid,from,to])` / `pwdRequest(sid)`

关键语义:

- **attach 回放顺序**:`Hello → Shells → ShellsConfig → ShellsMeta → HeadlessShells → ActiveShell → 缓冲 Chunks`;
  `ActiveShell` 是最后一个状态消息,前端触发 `onReplayComplete`(恢复编辑器/文件视图),故 `HeadlessShells` 必须在其前。
- `hello` = 服务器真实会话 key(= WS 路径 `{name}` = sessionStorage `sshweb-session-key`),前端据此拼 HTTP 下载 URL。
- `sftpOpenResult` 的 `sid` = 该服务器去重的 headless shell,前端按 `user@host:port`/`local` 绑定视图。
  第 4 元素 `notice` 除「已按配置用户目录打开」这类提示外,**也承载连不上目标的整段失败文案**:SSH 连不上时是
  「无法连接该服务器的 SFTP 服务,请稍后重试」,而**主机密钥不符时是 `ssh::HostKeyChanged` 的原文**(含两个指纹,
  见坑 80)——前端据此弹「重新信任」而非只弹 info toast。
- `sftpList` 第 4 元素 `truncated`:超 `MAX_LIST_ENTRIES`(20000)截断为 true;`SftpEntry` 的 `mode` 含文件类型位。
- `create` 第 4/5 元素 = `cwd`(可选初始目录)/ `label`(标签基名,`shellsMeta` 回放)。
- `ServerConfig.startupDir`(启动目录):**新建远程终端**默认取它作为 `create` 的 `cwd`;SFTP 探测(服务器列表 SFTP 按钮、
  终端首次打开)的起始浏览目录同取它——留空/`~` = SSH 登录主目录;支持**绝对路径或相对路径**,相对路径以该用户主目录为
  基准(`~/…` 或裸相对如 `project` 均按同一连接探测出的 home 展开为绝对路径,见 `utils::resolve_start_dir`;
  `runner.rs` 的远端 `cd` 对相对/`~/…` 保留未加引号的 `~` 交远端 shell 展开)。「在目录中启动 SSH」等显式传 cwd 的路径
  **覆盖**该默认。SFTP 列表路径必须绝对,故相对/`~/…` 只在有探测 home 处展开。
- **「本机」设置(`ServerSettings.local`,四项:启动命令 / 终端编码 / 家目录 / SOCKS5)不经 WS 协议下发,
  WS 协议对此零改动**:前端对本机(`server: null`)依旧只在 `create` 里发 `null` cwd,服务端读自己的加密配置兜底
  (见坑 81 为何不塞进 `ServerConfig`)。`GET /api/config` 回具体的 `LocalSettings`,`PUT` 的 `local` 段缺省则不改。
  四项语义与远程同名设置**一致**:`startup` 与 `ServerConfig.startup` 同一注入器(`SnippetInjector`,逐行敲入 +
  同一节奏)、`encoding` 与服务器表单的终端编码同一转码器(`Transcode`)、`home` 与「启动目录」同一展开规则
  (`utils::resolve_start_dir`,空/`~` → `$HOME`,相对路径按 `$HOME` 展开,绝对路径原样;本机另有 `is_dir()`
  校验,见坑 82)。本机**不做** su/提示符解析(远程专有),`pwdRequest` 仍由 `/proc/<pid>/cwd` 回答。
- `reorderShells` 需为当前 sids 的排列,否则报错且不广播。
- `sftpWriteAt` 分块上传(4MiB,offset 0 截断,Session FIFO 串行落盘;服务端再把一块切 256KiB 片**并发**下发,
  见坑 77);**完成判定见 `sftpUploadDone`/`sftpUploadOk`
  (坑 73)**:前端数完最后一块的 ack 后发 `sftpUploadDone([sid,path,total])`,服务端 stat 比对大小,相符才回
  `sftpUploadOk`,不符删半成品并报 `写入失败（path）：`.失败/取消走 `sftpUploadAbort`(坑 74)。`sftpCopy` 递归复制
  (跳 symlink/限深 32)。
- `ServerConfig`/`JumpHost` 的 `private_key`(服务端从 `keyId` 解析)`skip_serializing`,不出 WS/持久化。
- `pwdRequest(sid)` 让服务端报告 shell 的**当前实际工作目录**,回 `pwd([sid,path])`。**本地 shell** 读 `/proc/<pid>/cwd`
  (`Terminal::pid()` + `Session::set_shell_pid`,实时精确、无回显)。**远程 shell** 无法跨通道得知交互 cd 状态,由 runner
  **从 shell 提示符解析**(`feed_prompt_cwd`:逐行/尾部解析 `[user@]host:path[$#]` 或裸 `path[$#]`,path 可为 `~`/`~/…`
  ——提示符对 home 用 `~`),`session.report_pwd` 把 `~` 展开为 SFTP home。**零终端污染(不注入任何命令)**。提示符非标准
  (不含路径)时解析不到 → 回空,前端不传。

> 前端类型在 `src/lib/protocol.ts`,服务端在
> `crates/sshweb-server/src/web/protocol.rs`, 改协议两端同步。

---

## 五、已知坑与注意 (编号稳定,代码注释亦引用,勿重排)

> 仅保留**仍会踩**的问题(新功能/改动时需遵守)与**结构约束**(勿拆回)。已实现的功能特性见 §二/§四,不再赘述。
> 已移除的历史坑(编号空缺):1(库替换)、7(host key 已 TOFU 化,见 67)、10/16(连接统一)、18(目录起 shell)、
> 22(流式 zip)、23(自动建父目录)、26(上传 ACK)、33/34/35(流式归档/pendingReads/run_local)、
> 37(headlessui 已随自绘 `Portal`/`Dialog` 移除,2026-09)、39/42(runes 化消除的 legacy 坑,见下方头部注记)、41(会话持久化)、
> 61(空)、68/69(修复项,语义已入代码注释)。40 仍有效(DnD),勿视作空缺。
> 代码注释仍引用旧号,勿重排。

> ⚠️ **前端已升 Svelte 5 / SvelteKit 2 / Vite 6(2026-09),并已完成 runes 化迁移(2026-09-09,并入 dev 并部署)**:
> 全部 27 个组件 **0 `export let` / 0 `$:` / 0 DOM `on:*` / 0 `svelte:component`**(仅 `createEventDispatcher` 自定义事件仍保留
> `on:confirm`/`on:cancel` 等,及过渡事件 `fade/fly/scale`)。runes 迁移要点与逐组件提交见 git 历史 `9e3f2de..74f6225`(2026-09-09,已并入 dev)。纯 legacy 坑 **39/42** 已随
> runes 化消除 → 移入上方「编号空缺」(39=顶层声明求值序 TDZ;42=模板经 helper 读 store 不追踪,runes 由 `$derived`/`$effect`
> 及模板内直接 `$store` 订阅取代)。**43** 原为 legacy 语境的模板/表单限制,已改写为 runes 下仍适用的约束(TS `as` 禁;
> number 字段转数字;动态 type 输入仍手动 `value`+`oninput` 收口,勿退回裸 `bind:value`)。44(prettier3 +
> `.prettierignore`)及与后端/CodeMirror 语义相关者(38/47 等)不受迁移影响。改动前先核对代码。

**依赖 / 环境**

2. **legacy 语言模式需 tokenTable**:否则 StreamLanguage 高亮静默失效;动态 import 路径 vite 无法解析,必须写死
   (`languages.ts`)。
3. **russh 需 0.60**:SSH 传输层(终端/SFTP/代理/跳板)统一走 `crate::ssh::connect`,ssh-key fork 固定 `=0.6.18`,勿随意升级。
4. **WSL 无 systemd**:sshd 手动 `/usr/sbin/sshd -p 2222`;sshweb 服务手动 `nohup`/`setsid`。
5. **大文件 (>5000 行)编辑器降级是设计使然**,非 bug。
6. **本地文件管理操作的是服务端进程 cwd**,非浏览器主机目录。
8. **MAC 算法**:russh 仅支持 `hmac-sha2-512-etm/-256-etm/-512/-256/-sha1`;
   `hmac-sha1-96`/`hmac-md5` 前端可选但服务端忽略。终端与 SFTP 用同一 MAC 配置。
9. **终端编码语义**:encoding 表示远端 shell 输出编码;远端实际 UTF-8 却配 GBK 会丢弃非法序列(输出为空)。仅远端确为 GBK
   等环境时使用。

**并发/阻塞模型**

11. **本地文件操作必须 off-thread**:同步 `std::fs` 走 `tokio::task::spawn_blocking`
    (`run_local`/`sftp_archive_stream`)。**新增本地文件操作务必 spawn_blocking**。
12. **WS 消息循环不可阻塞**:`web/socket.rs` 单连接串行循环,任何 sftp 分支在其中 `await` 会卡住该连接所有消息。
    **所有 sftp 操作必须 `tokio::spawn` 独立任务**,错误经 `session.error()` 回传。
13. **发送端必须解耦**:socket `split()` 出独立 writer task + 单一 512 条有界队列(`try_send`,满丢慢客户端)。
    **勿把 send 搬回循环,勿拆双队列**。
14. **SFTP 池连接不得持锁 + 必须有超时**:`SftpPool` 锁外建连(快路径读锁、慢路径重查)、`CONNECT_TIMEOUT=25s`。**保持锁外连接 + 超时**。
15. **归档/复制须防符号链接环**:目录递归**跳过 symlink + 限深 32**,统一用 `sftp/local.rs::walk_local` /
    `sftp/remote.rs::walk_remote`(RemoteWalker visitor 必须 `Send`,运行在 `tokio::spawn` 内)。**新增目录递归请复用它们**。
17. **SFTP 复制/远程目录删除都跳过 symlink 并限深 32**:本地复制走 spawn_blocking;远程删除(`sftpRemove is_dir=true`)
    是递归的且子项**并行删除**(`remote.rs::remove_tree`,先删子项再删目录本身)。SFTP `rmdir` 只删空目录,勿退回单次 remove_dir。
19. **分块上传必须有序且不得丢块**:`sftpWriteAt` 走 Session 内**串行 FIFO 队列**落盘(坑 65 起该队列
    有界 `WRITE_QUEUE_CAP`,满时报「写入队列已满」而非静默丢块)。勿并发 spawn、勿拆 FIFO。上传的
    **完成校验**(`WriteOp::Finish`)与**失败清理**(`WriteOp::Abort`)也必须走这条队列(坑 73/74)——它们作用于同一个
    文件,越过队列就会「校验时还差最后一块」「删除后又被队列里的块写回来」。
20. **远程 SFTP 操作必须有硬超时**:`session::remote_timeout`(30s)包裹所有远程 sftp 操作,超时 `pool.invalidate()` 重连。
    **新增远程 SFTP 操作务必经它**。
21. **WS 输出队列非可靠确认**:512 条有界 `try_send`,客户端跟不上时任何消息可能被丢弃;勿当可靠确认机制。

**SFTP 语义**

24. **headless SFTP shell 不进标签栏**:`hidden` 标志,无 runner 任务,SFTP 池懒连接,按 host/port/username 去重;
    `shell_target` 遍历全部 shells 故 sftp 操作对 headless 生效。**新增 shell 用途时注意区分 hidden**。
25. **SFTP 半断连接清理**:连接关闭/失败由 `SftpPool::invalidate()`(drop 缓存 `Arc<Sftp>` 即关后台任务与 channel)清理;
    `ReadDir`/`Dir` 由库的 drop 自动关闭(消费式、drop 即关)。
27. **服务端部署边界**:release 二进制部署到**本机 WSL(`:2223`)与 201(`:40009`)两处**(见 §六);重启前用
    `ss -ltnp | grep ':端口 '` 取 PID(勿用 `pgrep -x sshweb`——该机常同时跑一次性实例,会一起命中),SIGTERM
    等待退出,再启动并检查端口/首页/WS。**勿把重启步骤作用到跳板机或目标 SSH 服务器**。
28. **页面认证与配置密钥**:首次启动须浏览器设置访问密码(最短 6 位);登录 token 只在内存,服务重启需重新登录;丢失密码无法
    从密文恢复 SSH 密码。勿在脚本/日志/文档写正式密码。
29. **前端不得混用 BigInt 与 number**:`srocket.ts` 统一 `Decoder({ int64AsNumber: true, useRecords: false, mapsAsObjects: true })`
    转 number。**新增自定义 Decoder 必须带 `useRecords:false`**(否则 CBOR map 解码成 Map,WS 消息被静默丢弃)。
30. **SFTP 身份不可跨终端复用 (重要认知)**:SFTP 走新 SSH 连接,只能以配置用户认证;终端 `su`/`sudo`、cd 状态跨通道不可见。
    SFTP 打开只判定「配置用户能否建 SFTP 会话」,勿做任何身份猜测。终端内 su 到的用户需配该用户条目用其 SFTP 按钮打开。
31. **SFTP 探测与列表一次连接**:`open_sftp_probe` 同一连接 `pwd; id -un` + SFTP 子系统,结果随 `SftpPool` 缓存;
    `invalidate` 同时丢探测结果。错误分两类:`Connect`(不可达/密码错)与 `NoSftp`(子系统被禁)。
32. **过期 SFTP 列表响应必须丢弃**:`FileManager.handleMessage` 只接受 `sftpList[0]===viewShellId && sftpList[1]===path` 的响应。
    **勿改成无条件刷新**。
36. **SSH 密钥是敏感数据**:私钥只存 `config.enc`,不得经 WS/日志/前端输出;`resolve_auth` 只在 key 模式写 `private_key`;
    被删/不存在明确报错不静默回退。生成用 `Ed25519Keypair::from_seed` 绕开 rand 版本不兼容;`PublicKey::to_openssh()` 已含
    comment,勿重复拼接。

**前端框架**

38. **前端上传失败解析耦合中文文案**:`upload.ts` 用 `/写入失败（(.+?)）：/` 正则解析失败路径——前后端唯一靠中文耦合,
    改服务端文案会静默破坏上传失败标记。属协议缺口,勿依赖该文案变更。**因此**:队列满(`坑 65`)与上传校验不符
    (`坑 73`)的服务端文案也一律带该前缀;而**不是**上传失败的错误(如编辑器保存被拒,坑 75)必须用**别的前缀**,
    否则按路径匹配会把健康的上传一起置失败。
40. **HTML5 拖拽(DnD)要点**:`droppable` 的 `dragover` 与 `dragenter` 都须 `preventDefault`(否则 drop 不派发 / no-drop
    光标);`draggable` 的 dragend 绑稳定闭包(Svelte action `update()` 每次重渲染调用);拖拽源元素须设 `draggable` 属性。
    (弹窗外点关闭已由自绘 Dialog 的遮罩关闭按钮处理,无需 mousedown preventDefault 技巧。)
43. **Svelte 模板/表单限制(字段驱动渲染;runes 下仍适用,非 legacy 专属)**:① 模板表达式**不支持 TS `as` 强制转换**
    (`(x as T)[k]` 报 Unexpected token,runes 亦然);需在 script 内 cast,或给表单类型加索引签名
    (`let form = $state<ServerForm & Record<string, any>>(blankForm())`)。② legacy 的「`bind:value` 不能配动态 `type`」编译期
    限制**在 runes 已解除**(动态 `type` + `bind:value` 可编译无告警);Servers.svelte 字段网格仍用 `value={form[key]}` +
    `oninput={(e) => (form[key] = inputValue(e, f))}` 手动写法,只为把 string→number 转换收口到 `inputValue` 单点——若改用
    `bind:value` 须自行保留 ④ 的数字转换,勿退回逐字段手写。③ `{@const}` 须是 `{#each}`/`{#if}` 块**第一个子节点**,把 `f.key`
    提为 `{@const key = f.key}` 再 `{#if key}`/`{:else if key}` 窄化,否则 `oninput` 闭包内 TS 丢失窄化。④ **number 字段必须
    转数字**:`.value` 是 string,直接赋 `form.port`/`proxyPort`/`socks5Port` → 服务端 `ServerConfig.port: u16` 反序列化失败
    → **一切含 port 的请求(PUT /api/config、install、test-connection)400**;`inputValue` 对 `spec.type === "number"` 返回
    `Number(value)`。
44. **前端格式与 lint**:`npm run check`(svelte-check)是绿门,但 `npm run lint` 整条**恒不过**——prettier3 默认按
    `lf` 判而工作区文件几乎都是 CRLF(全仓误报),且它后面的 `eslint`(单独跑 exit 0)因此根本没被执行。真实格式
    改用 `npx prettier --check --end-of-line auto .`(见 §三 验证/检查;它现在仍会报 4 个文件的**真实**漂移)。
    `.prettierignore` 忽略有意保留手写格式的文件(AGENTS.md / README.md / 开发记录文档.md / app.css / app.html)
    ——改动它们无需(也勿)跑 prettier;其余代码文件保持 prettier3 格式即可。eslint 负责 TS + svelte/valid-compile。

**结构约束(勿拆回/勿倒退)**

45. **终端启动命令必须逐行注入**:状态机节奏——普通行 350ms;`su - <user>`/`sudo -i` 行后 250ms 送密码行,密码行后 1000ms
    再续。整段写入会被 readline 吞掉导致 `su` 密码卡死(`runner.rs::user_switch_line` 识别,仅用于注入节奏,**与 SFTP 无关**)。
    `user_switch_line` 与输出扫描 `switch_user_from_tokens` 共用同一 token 解析(2026-08 收口),前者**要求 su/sudo 是行首
    首词**——勿改成任意位置匹配,否则 `echo su` 等 startup 行会误触发密码注入。
46. **文件管理右键菜单条目行必须 `event.stopPropagation()`**(runes 写法 `oncontextmenu={(e) => { e.preventDefault(); e.stopPropagation(); … }}`,勿再写 legacy 修饰符 `on:contextmenu|stopPropagation`):否则冒泡到列表容器(容器 handler 先 `ctxEntry = null` 再 `openCtxMenu`)清空 `ctxEntry`,右键文件夹「在目录中启动 ssh」会跑错目录。
47. **编辑器行 diff 用增量跟踪,勿退回整文 diff**:`diff.ts::applyDocChanges` 基于 CodeMirror `update.changes`
    (整文 diff 无法区分相邻空行/误标);`cm-diff-*` 只保留左侧竖条。
48. **`net_rates()` 单次采样共享时间戳**:CPU/网速的前后样本与时间戳必须同一次读取(否则第二次调用速率放大 ~1000 倍)。
49. **SFTP 编排 run_* 包装勿拆回**:`session/sftp_ops.rs` 的 `run_target`/`run_path_op`/`run_pair_op`/`run_path_ret`
    (含 `remote_timeout`/`run_local`/`run_and_ack`),以及 `with_remote`/`ensure_parent_dir`/`file_basename`/`zip_name`/
    `shell_quote`/`channel_event` 等收口助手,勿拆回各自重复样板(坑 11/12/14/19/20 语义在此单点)。
50. **`web/socket.rs` 的 spawn 包装为单一 `spawn_op`(勿退回 `spawn_ack`/`spawn_unit` 旧样板)**:spawn 闭包返回借用
    `&Session` 的 future 无法表达 HRTB 生命周期,现有 `spawn_op(session, label, create)` 已是最紧凑可编译形式。
51. **前端共享助手是单点,勿重复实现**:`lib/storage.ts`、`lib/clipboard.ts`、`lib/toast.ts::toastError`、
    `lib/ui/a11y.ts::noop`、`session/editors.ts::editorKey`(sid:path 复合键)与 `session/editors.ts::update`(tab 状态 patch 合并)、
    `session/stats.ts::prune`(关闭终端统计清理)、`connections.ts::splitProxy/joinProxy/
    splitSocks5Tunnel/joinSocks5Tunnel/effectivePassword`、`ssh.rs::AuthCredential::resolve`(key/密码判定)。重复实现会漂移。
52. **文件列表名字 span 与权限列勿加原生 `title`**:会遮挡自定义 hover tooltip;自定义提示经 `FileTooltip`。
53. **共享重构单点(勿拆回/勿倒退)**:① 拖拽重排统一 `dnd.ts::createReorderDnd<T>()`(服务端列表与终端标签条共用;
     `moveServer` 与 `Session.onTabDrop` 插入语义不同,只合并状态机、未合并算法,勿擅自统一)。② 表单输入统一全局
     `.input-base`(app.css;AuthGate 登录输入是独立实底变体 `.input-auth`)。③ 终端与编辑器细滚动条统一 app.css 的
     `.xterm-viewport, .cm-editor .cm-scroller` 规则。④ 拖放上传用 `webkitGetAsEntry` 递归(`upload.ts::readDropPayload`/
     `collectDropFiles`),`dataTransfer.files` 无 `webkitRelativePath` 且会把文件夹当 0B 文件,勿退回直读。⑤ 终端拖放上传
     目标 = 终端当前目录(本地 `/proc/<pid>/cwd`;远程提示符解析 `runner.rs::feed_prompt_cwd`,勿改回 PTY 注入)。
54. **`web/embed.rs` SPA fallback 的 content-type 必须随响应对齐**:fallback 返回 index.html 时**必须**用
    `text/html`(勿沿用请求路径的类型)。否则①根 `/` 被当成 `application/octet-stream` → 浏览器把页面当文件**下载**;
    ②缺失的哈希 chunk(旧缓存 index.html 引用的)被回退成 HTML 却带 `application/javascript` → 控制台
    `Uncaught (in promise) SyntaxError: Unexpected token '<'` + 页面卡死。另:带扩展名的真实文件请求缺失时返回 **404**
    而非 SPA shell(`looks_like_file`),避免旧哈希 chunk 渲染成 HTML;根 `/` 与无扩展名路由才回退 index.html。
    此 content-type 修复同时让 `no_cache_html` 中间件对 index.html 生效(此前 octet-stream 不触发)→ 前端重建后
    浏览器不再缓存旧版。用户遇「刷新变下载 + Unexpected token '<'」时优先检查此处,并需**硬刷新**(Ctrl+Shift+R)清旧缓存。
55. **FileManager follow-active 必须重估侧栏打开**:`FileManager.svelte` 的 follow `$effect` 除 `activeKey` 变化外,还要
    监听侧栏 `open` 的关闭→打开转换(`prevFollowOpen`)。否则终端在侧栏关闭时激活、之后打开侧栏,视图不会自动跟随
    (停在 `/`、刷新无反应,只有点服务器列表才恢复)。勿改回只监听 `activeKey`。
56. **CodeMirror 自定义搜索面板必须实现 `mount()`**:`CodeEditor.svelte::createSearchPanel` 返回的面板对象缺 `mount()`
    时,面板打开后焦点不落到搜索框(默认面板靠 `mount()` 聚焦)。保留 `mount(){ searchInput.focus(); searchInput.select(); }`。
57. **删除成功 toast 带文件名**:`FileManager.svelte::applyAck` 在全部删除 ack 后显示 `file.toastDeleted`(单文件 `{name}`)
    / `file.toastDeletedMany`(多文件 `{n}`),名字取自 `pendingDeleteNames`。勿改回无参固定文案。
58. **本地终端默认 shell 走 `$SHELL`/`/bin/bash`**:`state.rs::default_shell` 在未传 `--shell` 时优先用启动环境的 `$SHELL`,
    未设置则 `/bin/bash`。勿改回硬编码 `/bin/sh`(会让不带 `--shell` 的部署出现 `sh-5.0#` 裸提示符)。远程 shell 由远端
    `exec "${SHELL:-/bin/sh}" -l`(runner.rs)决定,与本地默认无关;**`-l` 见坑 76**。本地终端(`terminal/unix.rs`)同样
    以 `execvp(shell, &[shell, "-l"])` 起**登录** shell(坑 78),与远程保持一致。
59. **上传任务按 shell+path 复合 key**:`upload.ts` 的 `uploads` Map 用 `${targetShell}:${targetPath}` 作 key(原仅按 path,
    同路径跨服务器并发上传时后者覆盖前者 → 前者静默停止)。服务端错误消息只带 path(`写入失败（…）：`),同路径跨 shell
    无法区分,`onUploadError` 会标记所有同路径上传失败(保守,总比静默卡死好),勿改回单 key。
60. **上传 ack 必须回带偏移 + 看门狗重试**:服务端对 `sftpWriteAt` 成功后发 `sftpWriteOk([sid,path,offset])`(与普通 `sftpOk`
    区分);前端按 offset 去重(`ackedOffsets`),避免看门狗重试后重复 ack 造成跳块。每块 60s 看门狗(`UPLOAD_ACK_TIMEOUT_MS`),
    超时重发同 offset(幂等写),`UPLOAD_MAX_RETRIES`=3 后标记失败。**勿把 WS 输出队列(512 有界 try_send)当可靠确认**
    (坑 21)——上传推进只能以带偏移的 ack 为准。看门狗在**等待完成校验**阶段同样生效(重发 `sftpUploadDone`,重发幂等:
    再校验一次大小仍相等,再回一次 `sftpUploadOk`),否则最后一步丢消息会永久卡住任务(坑 73)。
62. **首次启动改用「安装密钥」登录并强制改密(H1)**:`ConfigStore` 首次启动生成一次性安装密钥(`config.rs::setup_key`,
    启动时打印到日志,仅打印一次,不落盘)。未设密码时 `/api/auth/login` 只接受安装密钥,登录会话标记 `pendingChange`,
    只能调用 `/auth/password` 设置访问密码(旧 `/api/auth/setup` 端点已删除);设密后密钥作废。**勿恢复无密钥的 setup
    端点**——那是安全审查 H1(首配接管)的根因。
63. **登录/改密按来源 IP 限流(M1)**:`web/ratelimit.rs` 滑动窗口(10 次/15 分钟),超限返回 429。**勿移除**——访问口令
    是唯一凭据,无限流可被在线爆破。限流需要 `ConnectInfo`,listen 用具体 `NoDelayListener`(替代 axum `tap_io`),
    勿改回泛型 Listener。
64. **HTTPS 支持 + Secure Cookie + HSTS(M2)**:`--tls-cert`/`--tls-key` 启用 HTTPS(rustls ring provider,`tls.rs`),
    此时 Cookie 加 `Secure`、响应加 HSTS、`config.set_secure_cookies(true)`。非回环地址明文监听会打印启动警告。**勿把
    Secure Cookie 无条件打开**(纯 HTTP 部署会登不进);勿移除 `tls.rs::PeerInfo`(孤儿规则要求本地 connect-info 类型)。
65. **服务端资源上限(M4)**:每会话最多 `MAX_SHELLS_PER_SESSION=64` 个 shell;文件写队列改有界
    `WRITE_QUEUE_CAP=256`,`enqueue_write_at/write/upload_done/upload_abort` 满时 `try_send` 失败并报
    「写入失败（path）：写入队列已满」(带该前缀才能被前端识别,坑 38);WS 单帧上限 16 MiB;
    上传跟踪集合 `MAX_TRACKED_UPLOADS=4096`(坑 75)。
    **勿改回无界队列**(安全审查 M4:恶意客户端可无界堆积);合法客户端每文件逐块发送不会触顶。
66. **SSH 会话回放脱敏密码(M6)**:`session.rs::redact_credentials` 在 `ShellsConfig`/`HeadlessShells` 回放前清空
    password/private_key/跳板与代理口令;前端 `runtime.ts::mergeReplayConfig` 按 `serverTargetKey` 从本地 `servers`
    缓存合并回密码(本浏览器提交过的才有)。**勿在回放里回送口令**——否则任何 attach 该会话的客户端免费拿到全部 SSH 密码。
    `/api/config` 仍回送密码(前端连服务器需要),属设计边界。
67. **SSH host key TOFU(H2)**:`ssh::connect(server, host_keys)` 首次连接成功后在 `config.enc` 记录目标
    `user@host:port` 的 SHA256 指纹,后续连接校验;不匹配即拒绝。`SshHandler` 用共享 `Arc<Mutex>` 捕获实际密钥,
    **勿改回无条件 `Ok(true)`**。跳板机仍不校验(仅主目标),属已知取舍。**指纹只在首次连接写入,故重装过系统的
    目标会被永久拒绝——处理见坑 80**。
70. **`crypto.randomUUID()` 仅在安全上下文存在(纯 http LAN 部署会崩)**:`window.crypto.randomUUID` 只在 HTTPS 或
    localhost(安全上下文)可用;经 `http://<局域网IP>:端口` 访问时其为 `undefined`,直接调用抛
    `TypeError: crypto.randomUUID is not a function`(本项目 WSL/201 均为纯 http,曾致登录后某按钮流程报错)。
    **一切 id 生成走 `lib/uuid.ts::uuid()` 单点**(有 `randomUUID` 用之,否则 `crypto.getRandomValues` 拼 RFC-4122,
    极端环境退 `Math.random`);勿再直呼 `crypto.randomUUID()`(`Session.svelte` 会话 key、`connections.ts`
    addServer/duplicateServer、`legacy.ts` 迁移均已改走 `uuid()`)。
71. **SFTP 复制 =「合并」覆盖 + 分块流式 + 断点续传(无整树 30s 墙)**:`sftp/remote.rs`
    复制不再整树被 `remote_timeout` 30s 硬超时包裹(坑 20):一次复制可跑任意时长;每个
    SFTP 请求(stat/mkdir/read/write)**各自 30s 看门狗**;传输中请求中断/超时 → `pool.invalidate()`
    重连,**从该文件已拷 offset 续传**(每文件最多重试 3 次)。语义:目标同名目录保留并**合并**
    (逐文件覆盖、保留多余文件),非目录占用名先删再建;目录创建/列表阶段失败则整体失败
    (不跨目录续传,文件名/软链跳过与 depth 32 限制沿用)。`ssh.rs` 已开 keepalive(30s×3)。
    进度经 `sftpCopyProgress([sid,from,bytes])` 节流(约 1MiB)上报,前端文件管理头部显示
    轻量进度行;小文件树(字节 < 阈值)不显示进度行,以成功 ack(`复制成功 N 项`)为准。
    **注意**:keepalive/看门狗只防「传输中断」,真死隧道仍按请求粒度超时重连,不会无限 hang。
72. **密码输入必须在 `<form>` 内且带 `autocomplete`(Chromium 两条控制台提示)**:密码框不在表单内报
    "Password field is not contained in a form";进表单但缺 autocomplete 再报 "Input elements should have
    autocomplete attributes (suggested: ...)"。修复范式已落地:`Servers.svelte` add/edit 弹窗整段 `<form onsubmit>`
    (保存钮 `type=submit`、其余按钮显式 `type=button` 防默认 submit 误触)、`Settings.svelte` 改密区、
    `PromptDialog`(输入包 form,Enter 原生提交,Escape 走 Dialog 全局)。**autocomplete 语义**:
    本站访问密码(登录/改密)用 `current-password`/`new-password`;远端服务器/跳板/代理/SOCKS5/安装密钥等
    **非本站凭据统一 `off`**(同 AuthGate setup-key),避免浏览器密码管理器接管/保存外部密钥。**新增带密码输入时
    请包进 form 并给对 autocomplete**,勿只加单条压制。
73. **上传完成必须由服务端按大小校验,不能数 ack**:`sftpWriteAt` 每块独立落盘,「最后一块 ack 到了」只证明
    那一块的 write 返回了,**不证明磁盘上是完整文件**——丢块、连接中断、断点错位都会留下一个长度偏短、却
    没有任何写失败的文件(用户侧的典型现象是 `pg_restore: error: could not read from input file: end of
    file`、jar 起不来、sh 报语法错)。故前端在最后一块 ack 后发 `sftpUploadDone([sid,path,total])`,服务端
    `sftp_upload_finish` stat 后比对:**不符即删除半成品**并走 `写入失败（path）：` 通道报错(客户端据此把任务
    置失败并清理);相符才回 `sftpUploadOk([sid,path,size])`——**只有它代表上传成功**(`upload.ts::onUploadVerified`)。
    前端完成判定同时按**字节**(`nextOffset >= total`)而非 `ceil(size/块大小)` 个 ack(短块会让 ack 数先到齐)。
    **勿把成功判定退回「ack 数够了」。**
74. **失败的上传必须删除半成品**:`sftpUploadAbort([sid,path])` 让服务端删掉未完成的文件,与分块**共用同一条
    Session 写 FIFO**(`WriteOp::Abort`/`Finish`),否则删除会越过仍在队列里的块、被那块重新写出来。前端在
    看门狗超时/错误/取消时发送(cancel 也走它,不再用 `sftpRemove`);**仅在「已经发出过至少一块」(
    `wroteAny`)时才发**——否则会删掉用户原本就存在的同名文件(offset 0 的块已经把原文件截断,故发过块就无此顾虑);
    被同路径新上传取代时**不发**(新上传的 offset 0 块会覆盖,abort 反而会删掉新文件)。页面刷新导致的中断**无法**
    清理(前端拿不到旧 File 也拿不到旧 sid),任务列表只标记「已中断」——用户重传同路径即可覆盖。
75. **上传未完成时禁止同路径的整文件保存**:编辑器保存(`sftpWrite` 整文件截断重写)若落在两条上传块之间,后续块
    按 `truncate(false)+seek` 写入 → 文件「长度对、内容是编辑器内容 + 空洞」。写 worker 用 `uploading` 集合
    (offset 0 的块登记、`Finish`/`Abort` 注销、上限 `MAX_TRACKED_UPLOADS`)拦截,报**`保存失败（path）：`**——
    **刻意不用「写入失败」前缀**:前端 `onUploadError` 按路径匹配该前缀并失败上传,复用会把健康的上传一起杀掉(坑 38)。
76. **在目录中启动的远程终端必须是登录 shell**:`runner.rs` 带 cwd 的分支走 `exec "${SHELL:-/bin/sh}" -l`(cwd 由
    `cd … &&` 完成)。**`-l` 不能省**:不带它是**非登录 shell**,不读 `/etc/profile`、`/etc/profile.d/*.sh`,于是
    Debian 系把 `/usr/lib/postgresql/*/bin` 之类加进 PATH 的脚本全部失效,而 `request_shell`(无 cwd)走 sshd 登录
    shell 有完整环境——同一次连接两条路径环境不同,表现为「sshweb 里某些命令找不到,普通 SSH 工具正常」。找不到的
    命令还会落到 Debian 的 `command-not-found` 处理器上,该脚本自身崩溃并打印 `UnboundLocalError: local variable
    'cnf' referenced before assignment` 之类 traceback,**把真正的 command not found 盖掉**(排查时勿被 traceback 带偏;
    `ps -p $$ -o args=` 看 argv[0] 是否以 `-` 开头即可判定登录与否)。另注:同一台服务器若配置了**启动命令**
    (`ServerConfig.startup`,逐行注入终端),它同样会改变后续环境,排查 PATH 类差异时一并确认。
77. **上传必须并发下发多个 SFTP 写请求,否则被 RTT 拖死**:`openssh-sftp-client` 的每个 `File::write` **各自等自己的
    回复**(`file/mod.rs` 的 `send_writable_request(…).wait()`),所以单个句柄的写入速率 = `每请求字节 / RTT`;再叠加
    客户端「一块在途、等 ack 才发下一块」,整条链路是**两级停等**。实测(本机 → 目标 20ms RTT)240KiB 块只有
    2.9 MiB/s,而同一链路上 OpenSSH `scp`/`sftp put`(多请求在途)是 23~24 MiB/s。修法:`sftp/remote.rs`
    `write_at_remote` 把一块切成 `PIECE_BYTES`(256KiB,≈OpenSSH 通告的 `max-write-length`)的片,**每片一个句柄**
    (`File::write` 要 `&mut self`,并发就得并发句柄)在 `MAX_PARALLEL_WRITES=16` 内并发写,同一 SFTP 通道上多请求
    自然在途;`UPLOAD_CHUNK` 同时由 240KiB 提到 4MiB。改后同条件 4MiB 块 **21.6 MiB/s**(16MiB 文件 0.74s),局域网
    本机 1MiB 块 110 MiB/s。**两条约束勿破坏**:
    - **只有 `offset == 0` 的块先单独、同步地 `truncate(true)` 建/清文件**,之后才允许其它片写:后续片全部
      `truncate(false)`,若某个 truncate 落在它们的写之后,那一截数据会被抹掉(表现为文件长度对、内容坏);
    - 片句柄**出错也要 `close`**(`File` 无 `Drop`,丢弃的句柄不会发 CLOSE,会一直留在目标机上直到通道断开);
      `write_piece` 因此只 seek+write,由调用方负责关闭。
    另:`WRITE_QUEUE_CAP` 随块大小由 256 降到 64(`session.rs`),队列按**条数**限内存,块大 16 倍后同样的条数
    意味着 16 倍内存。
78. **本机终端也必须是登录 shell**:`terminal/unix.rs::execv_child` 以 `execvp(shell, &[shell, "-l"])` 起 shell(原为
    `execvp(shell, &[shell])`,**非登录**)。与坑 76 同一个根因、同一个后果:本机 SFTP 面板「在目录中启动 ssh 会话」
    建的本地终端不读 `/etc/profile`、`/etc/profile.d/*.sh`,由 profile.d 追加的 PATH(数据库客户端、语言工具链)全部
    丢失,而 `ssh localhost` / 桌面终端有完整环境。`-l` 对 bash/dash/zsh/ksh/fish 通用;等价的前导 `-` 约定
    (`argv[0]="-bash"`)对 bash/dash 成立但 fish 不认,故统一传 `-l`(nix 的 `execvp<&CStr>` 需要 `c"-l"`)。
    **勿改回不带 `-l`**;本机终端与远程终端的环境自此一致。**(后续补充)** 本机终端此后还会读「本机」设置里的
    终端编码、启动命令与家目录(坑 81/82),这三项与远程同名设置走同一套实现、语义一致;`--shell` 仍是进程级
    启动参数(可执行文件),与「启动命令」(启动后逐行敲入的片段)是两回事。
79. **下载必须并发下发多个 SFTP 读请求,否则被 RTT 拖死(坑 77 的下载孪生)**:`openssh-sftp-client` 的每个
    `File::read` 同样**各自等自己的回复**;更早的实现(`web/download.rs::download_stream` + `sftp/reader.rs`)是
    「一块 256KiB 读回来、写出去、再读下一块」的**纯停等**,单连接下载速率 = `256KiB / RTT`。实测(本机 → 目标
    20ms RTT,同一文件 16MiB):单请求整流 **5.29 MiB/s**(≈ 预测值 6.6 MiB/s),同链路 `scp` **17.1~17.8 MiB/s**,
    16 条独立 HTTP/SFTP 通道并发 **23.25 MiB/s** —— 通道与目标都够快,瓶颈就是停等。修后同条件 **15.76 MiB/s**
    (≈ `scp` 的 88%),16 路并发分片 20.33 MiB/s,首 1MiB 由 292ms 降到 187ms;在途提到 16 反而略慢(14.97 MiB/s),
    故 `MAX_INFLIGHT_READS` 定在 8。**若把 RTT 拉到 200ms**(同链路 `scp` 4.15 MiB/s),修前同样受 `256KiB/RTT` 限制,
    修后单请求 4.05 MiB/s 已**与 scp 持平**——即瓶颈从停等回到了链路本身,再加大在途也无收益。修法:`DownloadReader::Remote`
    改为**预取队列**——每轮 `fill` 提交 `MAX_INFLIGHT_READS=8` 个 256KiB 读(**每片一个 `File::clone()`,句柄共享、
    偏移本地**,所以各片独立在途)后一次 `try_join_all`,读到的片按序入 `VecDeque`,`read()` 只从队列取(调用方缓冲区
    小于一片时余料留在队首)。`download_stream` 的 `CHUNK` 与片大小对齐(256KiB),于是每个 HTTP chunk 都是一片**已经在
    内存里**的数据,不再等 RTT。**约束**:
    - `File` 的 `read`/`seek` 要 `&mut self`,并发只能靠 `File::clone()`(`file/mod.rs` 有 `impl Clone`);
    - **短回复必须收缩 `piece`**:服务端 `max-read-length` 小于 256KiB 时会返回更少字节;此时本轮的其余片是按「整片」
      推算的偏移,必须丢弃并从该偏移以新片长重发(实现里 `*piece = got as u32` 后 break),否则文件出现空洞;
    - **没有进展必须结束区间**:`fill` 读完一轮若一个可用的字节都没拿到,把 `end` 收到 `next`,避免调用方的
      `while ready.is_empty() && next < end` 空转;
    - 打开时**不预读**(`reader_remote` 不读字节),`length` 由 handler 传入并 `min(size)` 收紧,避免读越过 Range 末尾;
    - 片句柄仍由 `OwnedHandle::Drop` 负责 CLOSE(与坑 77 的 `write_piece` 不同,读路径不需要显式 close)。
80. **重装/换过系统的主机 = TOFU 永久拒绝,必须给「重新信任」的出路**:TOFU 校验(坑 67)把首次连接成功时的
    主机密钥指纹记进 `ServerSettings.host_keys`(`user@host:port` → SHA256 指纹),此后不符即拒。**该指纹只在「首次
    连接」写入**,所以目标重装系统(或换密钥)后**没有任何自动恢复路径**:每次连接都在密钥交换阶段失败,russh 只报
    一句 `Unknown server key`(`Error::UnknownKey`),用户既不知道是主机密钥问题、也与自己上传的登录公钥毫无关系
    (失败发生在认证**之前**)。真实案例:201 上的「中广核」重装后报 `连接失败:SSH connect failed: Unknown server
    key`,而同一条 PC 上的 `known_hosts`(浏览器/PC 侧)与 201 的 `host_keys`(服务端侧)是**两个互不相干的记录**。
    修法三层:
    - **错误里带指纹**:`ssh.rs` 在 `client::connect*` 失败后,把 `SshHandler` 本次实际看到的指纹与期望指纹比对,
      不符则用 `HostKeyChanged { target, expected, actual }` 顶替原错误(其 `Display` 明写「…的 SSH 主机密钥已变更:
      此前记录 X,本次为 Y。若该服务器重装过系统或更换过主机密钥,请确认后重新信任」)。**不是**去 downcast russh 的
      `UnknownKey`:`client::connect<H>` 的错误类型是 `H::Error`(= `anyhow::Error`),原始变体不可靠可还原;比对指纹
      既版本无关又更有信息量。`check_server_key` 返回 `Ok(false)` 是 russh 抛 `UnknownKey` 的**唯一**原因,故这个
      判据不会误报(有单测覆盖:不符 / 无关失败 / 首次连接三种)。
    - **清除入口**:`ConfigStore::forget_host_key(target)`(删一条并持久化)+ `POST /api/host-keys/forget`(需认证,
      204/404/400/401)。**这是唯一的出路**,故前端不给「一键」:必须经弹窗看到两个指纹后确认。
    - **UI**:`HostKeyDialog.svelte`(挂在 `+layout.svelte`,z=95,唯一实例)+ `hostkey.ts` 的 `hostKeyPrompt` store。
      测试连接走结构化 `hostKeyChanged`(`api.ts::ApiError` 保留 `err.body`);终端与 SFTP 是**纯文本**通道,靠
      `hostKeyPromptFromMessage` 匹配标记串 `"的 SSH 主机密钥已变更"` 识别——与坑 38 的字符串耦合同类,且
      `i18n/index.ts` 已声明「服务端消息保持原文」。三个入口各自补齐(缺一个用户就会「有的入口没有出路」):
      ①`Servers.svelte` 测试连接(结构化);②`FileManager.svelte::applyError`(终端建连失败的 `error` 文案,
      形如 `连接 <名> 失败：<target> 的 SSH 主机密钥已变更…`);③`session/runtime.ts::applySftpOpenResult`
      (SFTP 探测失败的 `notice`,坑:后端原先把它换成了一句笼统的「无法连接…请稍后重试」,故 `sftp_ops.rs::
      send_probe_failure` 改为**主机密钥不符时原样透传**该错误)。命中弹窗时**不再另弹 toast**(文案重复且没有出路),
      但其余错误处理路径不变。
      **`target` 必须解析成裸 `user@host:port`**:终端文案带 `连接 X 失败：` 前缀,故取标记串**之前最后一个**
      `user@host:port` 片段(正则 `([^\s：@]+@[^\s：]+:\d+)\s*$`),否则会把前缀一并当成 target → `forget` 404、
      弹窗看着能用实际没用(E2E 就是靠断言 `forget` 返回 204 而非 404 卡住这个的)。
      **未覆盖**:SOCKS5 隧道的 SSH 连接是**懒建连**(每条入站连接才 `get_connection`),主机密钥不符时报不到
      `/api/proxies` 的开启响应上,只会表现为隧道连不通——需要时再单独处理。
81. **「本机」设置必须存在服务端加密配置里,且 `save_settings` 的手写部分合并要同步扩展**:本机可编辑的四项
    (启动命令 / 终端编码 / 家目录 / SOCKS5)是**服务端状态**,存 `ServerSettings.local`(`#[serde(default)]`,
    与 `keys`/`host_keys` 同一套向后兼容约定)。**不要**塞进 `create` 消息的 `ServerConfig`:本机没有主机/端口,
    要塞哨兵主机名,而 `ServerConfig::target_key()` 会得到 `"@:0"` ≠ `"local"`,连锁破坏
    `serverTargetKey(null) === "local"`(前端键绑定、文件管理器视图键、`ShellsConfig` 回放、代理注册表键全部错位)。
    因此 **WS 协议零改动**,前端对本机继续发 `null`。三个必须同时做到的细节:
    - **`save_settings` 是手写的部分合并**,只写 `servers`(`keys` 还要特判非空)。新增顶层段不写进合并就被
      **静默丢弃**:`PUT /api/config` 回 **204**、前端以为保存成功,刷新后本机设置回到默认值。也**不能**照抄
      `keys` 的「非空才合并」——四项全清空是合法操作。做法是 PUT 单独收 `SaveSettingsRequest { servers,
      #[serde(default)] keys, #[serde(default)] local: Option<LocalSettings> }`: `local` 为 `None`(缺省)= **不改**
      ——这个前端有「陈旧标签页」前科(坑 80 的 AuthGate 不重载),旧标签页只带 servers 的 PUT 不该抹掉本机设置;
      `GET` 仍回具体的 `LocalSettings`,前端不必判 null。读取走 `ConfigStore::local_settings()`(内部免鉴权路径,
      与 `host_key_for` 同类);**不在 `Session` 上缓存**:session 跨浏览器刷新长期存活,快照会让「改了设置没生效」。
    - **前端 `connections.ts` 有四处在丢字段,必须同时修**(只修一处等于没修):①`normalizeSettings` **重建**
      `{servers}` 对象(连 `local` 一起丢)→ 改为保留;②`deleteServer` 是**唯一没有 `...current` 展开**的写入
      (add/update/duplicate/move 都有)→ 补上,否则**删一台服务器就顺手抹掉本机配置**;③`persist()` 把 store 设成
      `next`,故 store 形状必须**始终**带 `local`;④`loadServers` 在 `remote.servers` 为空且无 legacy 存储时最后
      只调 `clearLegacyServers()` 就结束——`remote.local` **根本没落地**(没配任何服务器时本机设置一律显示默认值)。
    - 单测覆盖:`config.rs::tests::save_settings_keeps_sections_the_payload_omits`(带 local 保存 → 重开
      `ConfigStore` + 登录后仍在 → 不带 local 的 PUT 不清空 → 显式改/清空生效 → `keys`/`host_keys` 未受牵连)。
82. **本机家目录必须先 `is_dir()` 校验再交给终端,否则是静默死亡**:`terminal/unix.rs::execv_child` 的 `chdir`
    失败 → 返回 `Err` → 子进程 `exit(1)` → PTY 立刻 EIO → 终端 tab **闪一下就没了,用户看不到任何原因**。
    所以配置的家目录展开后若非目录,只 `warn!` 记一行日志并退回默认目录,**不要**改 `execv_child` 去加错误上报
    (那会动到所有本机终端的启动路径)。相关两个语义:
    - `utils::local_home()` **故意读 `$HOME`、不查 `/etc/passwd`**:每个 shell 都是本进程的子进程、继承本进程环境,
      终端里的 `~` 展开的就是这个 `$HOME`,换其它来源会得出一个 shell 自己不认作 home 的目录。
    - 显式 cwd(文件管理器「在目录中启动终端」)永远优先且**原样透传**:它指向用户刚在列表里看到的目录,语义必须与
      远程一致;`resolve_local_start_dir(explicit, home)` 的返回值 `None` = 「没有起始目录」(= 改动前的行为)。
    - 前端**不改**:`Session.svelte` 对本机仍传 `null` cwd(单一事实来源在服务端),显式目录优先级更高。
83. **本机直连 SOCKS5 只绑 `127.0.0.1`,勿加监听地址选项**:与既有隧道同一条理由(见 `proxy.rs` 模块注释):
    原生 TCP 端口无法复用 HTTP Cookie 认证,loopback 是安全底线;**多一个「绑定地址」字段就能把它变成局域网
    开放代理**——这是最可能被后人「顺手加上」的东西,故写进代码注释与本节。配套约定:
    - 入站认证可选(空用户名 = no-auth),但 UI 必须提示「本机任何能访问 `127.0.0.1` 的程序都能用这个代理」;
      **不随服务启动自动开启**(隧道也没有)。
    - 直连的 `CONNECT` 由 **sshweb 主机自己解析域名并建 TCP 连接**,不建任何 SSH 连接(这正是直连的意义),
      与 SSH 路径**相反**——写进注释免得后人「统一」掉。DNS/连接超时共用 `CONNECT_TIMEOUT`。
    - 直连与远程隧道**共用同一个 `ProxyRegistry`**:第二个注册表会让端口分配器打架——显式端口的冲突检测与从
      `PROXY_PORT_START`(10801)起的自动探测都必须看到**全部**已占端口。实现上抽 `ProxyUpstream::{Ssh,Direct}` +
      私有 `insert_and_spawn(...)`,两个公开入口 `start(server, port)` / `start_direct(tunnel)`。
    - REST 用**专用端点** `POST /api/proxies/local`,**不**把现有 `POST /api/proxies` 的 `server` 改成可选:后者会让
      「客户端漏字段」从今天的 422 变成「起一个 no-auth 直连代理」这种静默且与安全相关的失败。
    - 停止入口**不能指望 `DELETE /api/proxies/{key}` 兜住本机**:同段的静态路径优先于参数路径,DELETE 必须
      另外挂在 `/proxies/local` 上,**否则 405 且不回落**(见坑 86)。前端两种代理共用 `stopProxy(key)`,
      拼出的路径天然就是这一条,所以前端零改动;`GET /api/proxies` 按 key 工作,零改动;`ProxyStatus.name`
      对直连给字面量 `"本机"`(服务端无 i18n,唯一消费者是 toast)。
    - 转发目标是两支不同类型(russh 的 direct-tcpip 通道流 vs 原生 `TcpStream`),`impl Trait` 在 `match` 分支间
      编译不过,只能装箱成 `Box<dyn ByteStream>`(复用 `ssh.rs` 已有的 `ByteStream` trait);装箱后仍满足
      `copy_bidirectional` 所需的 `AsyncRead + AsyncWrite + Unpin`。
84. **toast 折行/单击复制/悬停暂停 + 新建终端自动聚焦**(四个看着像小事、删掉就复发的地方):
    - **toast 溢出**:消息 `<p>` 原先**没有任何 `overflow-wrap`**;内层 `max-w-md`(448px)减去 padding、图标、
      关闭按钮与两个 `ml-3` 后文字只剩约 348px,而不含空格的长 token(`SHA256:…` 指纹、绝对路径、URL)不会断行,
      直接画到框外。修法是 `break-words`(= `overflow-wrap: break-word`,**只在放不下时才断**,中文/英文散文观感
      不变;**不用 `break-all`**,那会把正常单词切碎),`.toast-box` 再补 `overflow-hidden` 兜底。
    - **单击复制**:点击面 = 整条 toast,关闭/动作按钮上 `stopPropagation`;消息区做成真实 `<button>`
      (可聚焦、Enter/Space 触发),**不能**退回「整条点击消除」——`bec67b3` 当初**故意**把整条点击消除换成显式
      关闭按钮(无障碍),加一个 `on:click` 就把它退回去了。复制反馈走 toast **内部瞬时状态**(图标位换
      `CheckIcon` + 「已复制」约 1.2s),**不新开 toast**(复制 toast 又弹 toast 会套娃)。
    - **悬停暂停**:就地改 `expires`(`remainingMs` 记剩余)即可,**不能替换 toast 对象引用**——`ToastContainer`
      按对象身份消除(`t !== toast`)、`{#each}` 也以对象为 key。去重路径(`existing.expires = obj.expires`)要一并
      清掉 `remainingMs`,否则暂停中被同文案去重会拿到**过期的剩余时间**。
    - **新建终端不聚焦**:effect 是 `if (term && active) claimFocus()`,而 `term` 是**非响应式 `let`**,`$effect` 的
      唯一依赖是 `active`;`term` 又是在 `onMount` 里两次 `await`(动态 import + 等字体)之后才赋值。新建终端时该
      XTerm 一挂载就 `active === true`,effect 那一次运行时 `term` 还是 `null`,之后 `active` 不再变化 →
      **永不聚焦**(刷新页面恢复同因)。修法:加响应式 `let ready = $state(false)`,`term = new Terminal(...)` +
      `open()` 之后置 `true`,effect 改为 `if (ready && active) claimFocus()`——`ready` 翻转时才跑而 `term` 已就绪。
85. **`encoding_rs` 的 `_to_vec_` / `decode_to_string` 只写进「已有容量」,新 `Vec`/`String` 会吞掉全部数据**:
    本次给两个 runner 抽 `Transcode`(本机终端也支持编码)并补 GBK 往返单测时,单测直接失败 —— `encode` 返回**空
    `Vec`**。根因两条:
    - `Encoder::encode_from_utf8_without_replacement(&mut self, src: &str, dst: &mut [u8], last)` 收的是**切片**,
      `&mut out`(`out: Vec<u8>`)会退化成**零长切片**;
    - `_to_vec_` 变体与 `Decoder::decode_to_string(src, &mut String, last)` 内部是 `let capacity = dst.capacity();`
      —— **先取容量再写**,所以新 `Vec`/`String`(容量 0)一个字节都写不进去,而且返回 `EncoderResult::InputEmpty`,
      **看起来"成功"**(这就是它会长期潜伏的原因)。
    这是**自 `3dd6937 项目初始化` 以来就存在的缺陷**:任何非 UTF-8 服务器上,解码后的终端输出与用户输入**全部被
    静默丢弃**(现象是「能连上但不显示输出」/「打字没反应」,与坑 9 的编码配置完全无关,只是从没在 GBK 环境验过)。
    修法:先 `reserve()` 再调 `decode_to_string` / `encode_from_utf8_to_vec_without_replacement`(留够
    `max_utf8_buffer_length` / `max_buffer_length_from_utf8_if_no_unmappables`,再各自 `max(len*4)+8` 兜底)。
    **勿改回 `let mut out = Vec::new(); … &mut out` 的写法**;顺带两个既有约束不变:解码器必须**跨读持久**
    (多字节字符被 PTY 读切两半时不能烂),非转码分支必须**原样透传原始字节**(`from_utf8_lossy` 只用于扫描/解析)。
86. **axum 路由同段「静态路径优先于参数路径」:只挂 POST 的 `/proxies/local` 会让 `DELETE /proxies/{key}` 失效**:
    本机直连代理的停止路径写成 `DELETE /api/proxies/{server_key}`(`server_key = "local"`),单测/`curl` 一打就是
    **405 `allow: POST`**——路由树里 `/proxies/local` 与 `/proxies/{server_key}` 是**两个节点**,静态节点先匹配,
    方法不匹配时**直接 405**,**不会**回落到参数路由(想当然以为「参数路径能兜住同段静态值」就会踩这个)。
    危害不止是不通:405 的响应**没有 JSON body**,前端 `request()` 走 `readError` 的「非 JSON」分支,
    toast 只说「服务端返回了无效响应(服务端可能已过旧,请重启服务端后刷新页面)」——**指向服务端版本,指向错误方向**,
    而代理照旧在跑、端口照旧在听(现象:⚡ 关不掉)。修法:`.route("/proxies/local", post(start_local_proxy).delete(stop_local_proxy))`,
    两种代理共用同一个私有 `stop(state, key)`。
    防复发:`web.rs` 里的 `proxy_stop_routes_reach_their_handlers` 直接对路由表断言——未带 cookie 时
    `DELETE/POST /api/proxies/local` 与 `DELETE /api/proxies/{key}` 都应是 **401**(鉴权闸门先响),拿到 405 即路由没挂上。
    该测试用 `tower::ServiceExt::oneshot` 驱动 `app()`,故 `Cargo.toml` 的 `[dev-dependencies]` 加了 `tower`(feature `util`)。
87. **`axum` 的 `http2` feature 是纯死重量,但「要不要开 h2」其实由 ALPN 决定**(2026-09-18 依赖审计后移除):
    `http2` 把 hyper 的整个 h2 codec 链进二进制(**实测 −670 KB,`.text` −10%**),而本项目的两个监听者**都不可能**用到它:
    明文监听**不能**上 h2(浏览器不会对明文端点说 h2c),TLS 监听(`tls.rs`)从头到尾**没有设 `alpn_protocols`**,
    于是浏览器按 ALPN 协商不出 `h2`、只会走 http/1.1。**恢复它的唯一正确前提是同时加上 ALPN 支持**
    (`ServerConfig::alpn_protocols` 里带上 `h2`),否则就是「编了六百多 KB 的代码而永远不执行」。
    审计报告与逐项证据见 `.doc/dependency-audit.md`(不入版本管理);该报告同时列出其余 20 项按 ROI 排序的候选。
88. **`tokio-rustls` 的 `default-features = false` 是承重的,不是省体积的洁癖**:
    它的默认 feature 集会打开 `aws_lc_rs`,与本项目显式要的 `ring` **feature-unify 到一起**,rustls 于是同时存在
    **两个** crypto provider;`CryptoProvider::from_crate_features()` 这种情况下返回 `None`,任何
    `ServerConfig::builder()` 都会**在运行时 panic**(`CryptoProvider::get_default_or_install_from_crate_features`
    里的 `.expect()`),而**编译期一切正常**——这就是它作为地雷的形态。当前 feature 集 `["ring","tls12"]`
    正是 `tls.rs` 显式安装的那一套(`builder_with_provider(ring::default_provider())`),两者必须同步改。
    附带收益:把 `aws-lc-sys`(**要 cmake/C 工具链**的 C 库)整个移出构建图,`Cargo.lock` 少了 13 个 crate
    (`aws-lc-rs`/`aws-lc-sys`/`cmake`/`dunce`/`fs_extra`/`jobserver`/`pkg-config`/`h2`/`indexmap`/`equivalent`/
    `atomic-waker`/`rustls-pemfile`/`tokio-stream`),Windows 侧 `cargo test` 不再卡在本机 gcc 上。
    代价:丢掉 `aws-lc` 提供的**后量子密钥交换**(X25519MLKEM768)——纯 rustls 库用法下无所谓。
    ⚠️ 反直觉点:`tokio-rustls` 的默认集**不等于** `ring`;别因为「反正 LTO 会删掉」就还原默认集,还原的是 panic 风险不是体积。
89. **`build/` 里的每一个字节都等于二进制里的一个字节 —— 前端体积有双倍价值**(做前端优化的第一条心法):
    `web/embed.rs` 用 `rust-embed` 把 `build/` **原样**嵌进二进制,且内嵌的是**未压缩**字节。所以前端 bundle 的
    gzip 数字**只反映传输成本**,不反映产品体积;一个「gzip 只省 4.5 KB」的改动在二进制里可能省 172 KB。
    三处按此口径取得收益(合计约 −397 KB 二进制):
    - `vite.config.ts` 的 `esbuild: { legalComments: "none" }`:`lucide-svelte` 给**每个**图标模块都带一份 ISC
      许可证横幅,默认的 `inline` 模式把它**抄进每一个 chunk**,48 个图标 = 172 KB。本应用只服务自己的用户,
      无「分发 bundle」的许可证展示义务,故去掉(**−172 KB**)。⚠️ 这是唯一一处「gzip 收益很小(4.5 KB)但
      二进制收益很大」的改动,不要用 gzip 数字判它的价值。
    - `src/app.css` 的 Inter 只留 latin / latin-ext 两个 `@font-face` + `unicode-range`(**−86 KB**):
      `+layout.svelte` 原先整个 `import "@fontsource-variable/inter"`,那会带进 7 个子集。浏览器**只下载命中的子集**,
      于是这条同时省二进制和省传输(实测加载一个全 ASCII 页面只请求 `inter-latin`,latin-ext 根本没发出去)。
    - `src/app.css` 删掉 `FiraCode-VF.woff`(**−139 KB**):`.woff` 是 IE11 世代的回退,现代浏览器**全都**支持 woff2,
      留着就是「139 KB 的二进制,没有任何浏览器会下载」。改 `@font-face` 时**别顺手把 woff 加回来**。
    ⚠️ `src/app.css` 在 `.prettierignore` 里,是**故意**的。
90. **终端 addon 必须停在 `@xterm/addon-*` 的 5.x 世代;TypeScript 侧要靠 `tsconfig.json` 的 `paths` 把
    `@xterm/xterm` 指向 fork**(两件都会「看着对但其实是错的」的事):
    - **版本号怎么选**:同名包有两个世代(`xterm-addon-*` 无 scope 已 npm-deprecated → `@xterm/addon-*` 有 scope),
      而**scope 化并没有停在 5.x**——`@xterm/addon-fit` 的**最新版是 6.x**,面向 `@xterm/xterm@6`,而 6.x **砍掉了
      canvas renderer**,正是本项目有意使用的那一个(`ui/XTerm.svelte` 里 `fontFamily` 旁的长注释说明了为何不用 WebGL:
      让 CJK 双宽格保持固定网格)。所以本项目的 pin 是 5.x 世代的**最后一个 build**:`addon-fit 0.10.0` /
      `addon-web-links 0.11.0` / `addon-image 0.8.0`,**精确版本、不加 `^`**。⚠️ 判据是 **peer range**(它才写明了
      面向哪个 kernel 世代),不是「数字看起来更大就更新」——升到 6.x 不是升级而是**功能回退**(终端变空白或渲染错格)。
    - **类型怎么对上**:addon 的 `.d.ts` 从 `@xterm/xterm` 取类型;本项目内核是 `sshx-xterm`(xterm.js 5.2 的 fork),
      它**没有**真实 5.5.0 的 `input` / `attachCustomWheelEventHandler`,于是每个 `term.loadAddon(...)` 都过不了类型检查。
      **两种直觉修法都不行**(已用 TS 探针逐一证伪):① `declare module "@xterm/xterm" { export * from "sshx-xterm" }`
      ——**真实包自带的环境声明赢过本地环境声明**;② 把同样的声明放进 `src/types/` ——同上。可行的是
      `tsconfig.json` 里 `paths` 把 `@xterm/xterm` 指到 `src/types/xterm-kernel.d.ts`(`export * from "sshx-xterm"`)。
      ⚠️ 两个连带的坑:`paths` 是**替换**而不是与 svelte-kit 生成的那份**合并**,所以 `$lib`/`$lib/*`/`$app/types`
      必须在这里**重抄一遍**(漏了会以「找不到 $lib」的形式炸);`src/app.d.ts` 里那条 `fontfaceobserver` 声明随之作废删掉。
91. **依赖下线的三处零散收尾(每处都有一句「为什么能删」)**:
    - `lucide-svelte` → **`@lucide/svelte`**:npm 上 `lucide-svelte` 已 deprecated,官方包改名到 scope 下;bundle 不变
      (仍是 48 个图标,**唯一决定什么被 tree-shake 的地方是 `src/lib/icons.ts`**),只是不再吃 deprecated 警告。
    - `fontfaceobserver` → **`document.fonts.load()`**:`document` 的字体 API 是 Baseline Widely available(Chrome 35+),
      且 `app.css` 自己声明了字面,第三方的 observer 多余。⚠️ **语义差异只有在「字体不存在」时显现**:`load()`
      在字族不在 FontFaceSet 里时**以空数组 resolve**(只在真的抓取失败时 reject),所以「**空数组 = 原来的
      『字体不可用』**」——`XTerm.svelte` 里 `if (loaded.length === 0) throw` 就是这条,删掉它 toast 就再也不响。
      已在真实浏览器里验过(见 §六 的字体探针):命中字面返回非空、字族不存在返回 `[]` 而非 reject、Fira Code 的
      `i/W/M/./空格/l/@` 步进极差 0.0000px、无任何 `.woff` 请求。
    - `tokio_stream` → **`futures_util::stream::poll_fn`**:全项目只用了 `UnboundedReceiverStream` **一处**
      (`session/sftp_ops.rs`),而 `mpsc::UnboundedReceiver` **本身不是 `Stream`**(只有 `async recv`),所以用
      `poll_fn(move |cx| rx.poll_recv(cx))` 适配**公开的** `poll_recv` 即可。⚠️ `poll_recv` 收 `&mut self`,
      绑定**必须**是 `let (tx, mut rx)`(少了 `mut` 就编译不过)——这正是这次唯一一次编译失败。
92. **`npm run build` 永远产出「不同的」`build/`:SvelteKit 的 build id 是 `Date.now()`**(零源码改动也会如此):
    id 写进 `_app/version.json`,又被塞进 entry chunk 的内容哈希 → `entry/start.*`、`entry/app.*` 改名,
    凡按名 import 它们的 chunk(`nodes/1.*`、共享 chunk)再跟着改名。2026-09-18 实测一次「源码一字未改」的重建:
    **4 个 chunk 改名**(`start`/`app`/`nodes/1`/一个共享 chunk),`build/` 总字节 +12(全部来自 `.br`/`.gz` 预压缩变体
    ——同一 id 长度相同,未压缩文件字节数不变,所以**二进制体积也一样**,变的只是内容)。
    三条后果,每条都真的会咬人:
    - **必须重建二进制**:`rust-embed` 会检测到 `build/` 变了(`Compiling sshweb-server` ~58s),
      所以「改前端 → 忘了 `cargo build`」不会静默过关,但反过来「没改后端却重建」也会真的重编。
    - **§六 那句「比对新旧前端 `_app/immutable/...` 的哈希文件名」会全线误报**:名字**本来就**全变了。
      要比就比**构建 id**(`curl /_app/version.json`)或干脆用下面的脚本。
    - **`build/` 不可复现** ⇒ 部署前无法用「哈希有没有变」判断「这次重建到底改了什么」。
    为此加了 `.doc/tmp-buildid-check.mjs`:它把新 `build/` 归一化(build id + `__sveltekit_*` 标识符 + **自动推导**
    的改名 chunk 哈希)后与**线上正在服务的那份**逐字节比对,并**先解压 `.br`/`.gz` 再比**
    (直接按 utf8 读压缩字节会把不同读成相同)。**部署前跑**:`node .doc/tmp-buildid-check.mjs [端口]`,
    退出码 0 = 零功能差异。2026-09-18 用它验证了本次重建:`4 个改名 chunk` + `153/153 全部相同`。
    ⚠️ 该脚本需要一个**装着旧构建的实例**在跑才能对照;旧二进制备份正是干这个的
    (`setsid ./sshweb.old-<日期>-<用途> --config /tmp/x/c.enc --listen 127.0.0.1 -p 2224`),用完杀掉。
    **可选的正解**:`vite.config.ts` 里 `sveltekit({ version: { name: __APP_VERSION__ } })` 把 id 钉成
    `0.5.0-<commit>`,即「同一提交 ⇒ 同一字节」。本应用**没有**用 SvelteKit 的 `updated` store
    (全仓只有 `+error.svelte` 用到 `$app/stores` 的 `page`),所以钉住它没有副作用;做之前先确认这一点仍然成立。

> **87-96 这几条是 2026-09-18 的记账,只有文档侧编号**(87-91 = 依赖瘦身,92 = 前端重建的构建 id,
> 93-95 = 字号设置/菜单钳位,96 = 探针读 `stty size` 的时序坑)
> ——代码里的注释各自把「为什么不能还原」写全了(位置:`Cargo.toml` 的 `axum`/`tokio-rustls` 两处、
> `src/app.css` 的 `@font-face`、`vite.config.ts` 的 `esbuild`、`tsconfig.json` 与 `src/types/xterm-kernel.d.ts`、
> `src/lib/icons.ts`、`src/lib/ui/XTerm.svelte` 的三处/字号 effect 与 `fit()`、
> `src/lib/ui/ContextMenu.svelte` 的钳位与 `flipFlyout`),但它们**没有**写 `坑 87` 这样的反向指针。
> 原因:那批改动已构建并部署,二进制与工作区是有意一一对应的;往已发布产物的源码里补注释会让「部署的那份」
> 与「工作区」不再能逐字节对上。下次因别的改动重建时,可以顺手补上。

93. **Svelte 5 的 `$effect` 只订阅「这一次运行真的读到」的响应式值 —— 写在 `if` 里等于没订阅**
    (「设置里改了字号/主题,已开着的终端毫无变化,只有之后新建的才生效」的根因):
    - 根因:`XTerm.svelte` 的 `term` 是**普通 `let`**(不是 `$state`),而创建它的 `onMount` 前面有四个动态
      `import()` 要 await,所以 effect 的**第一次运行发生在 `term` 还是 `null` 的时候**。那一次
      `if (term) { …读 $settings… }` 里一个响应式值都没读到 ⇒ **这个 effect 什么都没订阅**,此后永不重跑。
    - 修法:把要读的值**提到 `if` 外面**(`const nextFontSize = $settings.fontSize;` …),守卫换成 **`$state` 的 `ready`**
      (`onMount` 最后一句置 `true`)。这正是同文件**焦点 effect** 早就用过的同一招(注解在 `ready` 的声明处)。
    - 一般化:**决定「effect 订阅了什么」的是它第一次运行读了什么**,不是源码里出现过什么。

94. **字号是「几何」改动:写完必须重新 `fit()`,并把新的行列数报给服务端**:
    - xterm 每次写 `options.fontSize` 都会**重新量字**;字宽一变,同一容器能放下的列/行数就变了。只改渲染不重算 ⇒
      前端按 86 列画、远端仍按 118 列折行(中文与长命令行错位最明显)。所以字号分支是
      `options.fontSize = x; void fit();`,而 `fit()` 的最后一步是 `dispatch("resize", …)` →
      `Session.svelte::handleResize`(只转发 **active** shell)→ `rt.send({ resize: [sid, { rows, cols }] })` →
      `session.rs::resize_shell → send_input_size → ShellData::Size` → `runner.rs`(本机 `Terminal::set_winsize` /
      远端 `window_change(cols, rows, 0, 0)`)。
    - **这整条链 2026-09-18 在真机上验过**(`.doc/tmp-fontmenu-e2e.mjs`):14px 基线 PTY `41×118` → 改 20px 后 xterm
      自己的模型 `123→86` 列、PTY 变 `29×88`;另有对照「同一条 `fit→resize` 的**窗口尺寸**变化」`29×88 → 22×55`。
      即字号那一步的 `fit()` 与容器尺寸驱动的 `fit()` 走的是同一条回传,没有字号专用的旁路。

95. **右键菜单按「量出来的」尺寸钳位;子菜单只在「翻过去真的有用」时才翻**:
    - 旧实现按**硬编码**尺寸钳位,而菜单高度取决于行数(实测 **366px**,旧假设只有 330px),于是靠下边右键时
      最后几十 px 仍然出界。现在 `ContextMenu.svelte` 先渲染再量(`getBoundingClientRect`),`placed === null`
      期间整块 **`invisible`**——**不能先画一帧在视口外**;`left/top` 从指针位置钳到
      `[EDGE_MARGIN, innerWidth/Height − 尺寸 − EDGE_MARGIN]`。
    - 子菜单(「新建」「上传」)默认向右展开(`left-full`);**右边放不下**且**左边更宽裕**时才翻成 `right-full`。
      两个条件缺一不可:只看「右边放不下」会让窄窗口正中的菜单无故向左翻。
    - **不需要竖向翻转**:钳位后整个菜单都在视口内,两个带子菜单的行位于菜单前 ~125px,~78px 高的子菜单出不了下边界
      (理由写在 `flipFlyout` 旁)。
    - ⚠️ 判「是否 flip」**不能读 `getComputedStyle(el).right`**:绝对定位元素上的 `auto` 会被解析成 **used 值**,
      `left-full` 与 `right-full` 都给出像素值(都「不是 auto」)。要读 class。

96. **探针读终端里的 `stty size`:上一轮的输出会立刻满足等待条件 —— 真实变化被读成「没变」**
    (2026-09-18 这轮 PTY 问题的**真凶全在探针侧,产品侧从来没有问题**):
    - `stty size` 的输出留在视口里,而 `.xterm-rows` 是「每个视口行一个 `div`」(短输出只填空白行、行数不增长),于是
      「取视口里最后一个 `N M` 行」在**上一次**的输出上**立刻**成立 ⇒ 返回旧值。表现极具误导性:字号改完、DOM 推出来
      的列数 `123→86`、抓包也确实发出 `{"resize":[3,{rows:29,cols:88}]}`,而「PTY 行列」前后两次都读 `41×118`
      ——看着像服务端把 resize 吞了,**其实 PTY 第一次就变成了 `29×88`**(后面另一段读到的正是这个值)。
    - 正解:**每次调用用不同的 marker 锚定**——`echo SSHWEBSZ<n>; stty size`,只认「最后一个 marker **之后**的第一个
      `N M` 行」。改完同一条链全部通过(见坑 94 的数字)。
    - 连带:抓 WebSocket 帧的**环形缓冲**(只留 6 帧)同样不能用「帧数 / 最后一帧」判断「这一步之后有没有新帧」
      ——**入队时打序号**,比序号。
    - 为定位这件事临时给 `resize_shell` / `send_input_size` / 本机 `set_winsize` 加过 `info!`:日志逐条
      `rows=29 cols=88` → `sent=true` → PTY 随之改变,**服务端这一段一直是好的**。
      一般化:**症状指向服务端时,先确认测量手段本身会不会说谎**(本次两次「BAD」都是探针的假警报)。

---

## 六、当前运行环境

- **sshweb (release,musl 静态)运行中**:`/home/sshweb/sshweb`
  (工作目录 `/home/sshweb`,`--listen` 默认 `::` 双栈,**默认端口 2223**;日志 `/tmp/sshweb/server-rel.log`,
  PID 文件 `/tmp/sshweb/server-rel.pid`,配置 **`/home/sshweb/sshweb-config.enc`**——由原 8051 实例的
  `./sshweb-config.enc` 迁移而来,服务器/密钥/访问密码保留)。**前端已嵌入二进制(`web/embed.rs`),任何前端或 WS 协议变更必须重建
  `cargo build --release --target x86_64-unknown-linux-musl --bin sshweb` 并重启,否则旧端不认新消息 → 终端空白/放置不生效
  (如 `unknown variant 'setActive'/'reorderShells'`)。**
>
> **产品已改名 `sshweb`**(原 `sshx`):二进制/crate/包名、默认配置 `./sshweb-config.enc`(已迁移)、默认日志
> `sshweb.log`。**改名时保留不改**:`sshx-server-config-v1`(AAD,改则旧加密配置无法解密)、`sshx-xterm`(npm 依赖)、
> `sshx-servers-store`/`sshx-local-secret-v1`(旧数据迁移键)、上游 `github.com/ekzhang/sshx` 链接。
> 项目目录已随改名迁到 `cat/sshweb`(Windows/WSL 同);201 部署目录未改名(`/home/sshx`)。
- **更新部署**(WSL 内):
  ```bash
  ss -ltnp | grep ':2223 '              # 从监听端口取 PID:该机可能同时跑着别的 sshweb 一次性实例,
  kill <pid> && 等待退出                # `pgrep -x sshweb` 会把它们一起写进 PID 文件
  cd /mnt/c/Users/root/project/cat/sshweb && cargo build --release --target x86_64-unknown-linux-musl --bin sshweb
  cp /home/sshweb/sshweb /home/sshweb/sshweb.old-$(date +%Y%m%d)   # 旧二进制留一份,便于回滚
  cp target/x86_64-unknown-linux-musl/release/sshweb /home/sshweb/sshweb
  cd /home/sshweb && ./sshweb -d --shell /bin/bash \
    --log /tmp/sshweb/server-rel.log   # 工作目录 /home/sshweb,默认端口 2223,父进程立即返回
  ss -ltnp | grep ':2223 ' | grep -o 'pid=[0-9]*' | cut -d= -f2 > /tmp/sshweb/server-rel.pid
  ss -ltn | grep 2223 && curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:2223/
  ```
  **必须先 kill 再 `cp`**:替换一个正在运行的二进制会报 `Text file busy`。回滚即 `mv` 回旧文件再按同样步骤起。
  **不要把重启步骤作用到跳板机或目标 SSH 服务器**。部署到 201 等旧 glibc 机器用
  **musl 静态版**(坑:动态版报 `GLIBC_2.29 not found`,见 §三 静态构建)。
  ⚠️ **动了前端就必须让浏览器硬刷新一次**——**「重新登录」不等于「重新加载前端」**:重启后已开着的标签页会弹
  登录框,而 `AuthGate` 登录成功只置 `ready = true`、**不 reload 页面**,于是该页继续跑重启前那份 JS:界面是旧的、
  消息却是新后端发的(2026-09-17 部署坑 80 后用户只看到带两个指纹的 toast、没有「重新信任」弹窗,就是这个原因——
  文案来自新后端,所以指纹齐全,只有出路没有)。HTML 已设 `no-cache`、资源是内容哈希、也没有 Service Worker,故
  **F5 即够**,Ctrl+F5 更保险。排查手段:比对新旧前端——`curl -s http://<host>:<port>/` 里 `_app/immutable/...` 的
  **哈希文件名**与本地 `build/` 是否逐一相同(内容哈希相同即字节相同),再确认承载改动的那条 chunk 在不在服务列表里。
- **201 部署**(旧 glibc 机,`Linx GNU/Linux 6.0.100 buster`):经跳板 `172.18.237.219`(hostname wiscom,key 在
  Windows `C:\Users\root\.ssh\id_rsa`,已复制到 WSL `~/.ssh/`);`ssh -J 172.18.237.219 172.18.237.201`。sshweb 目录
  `/home/sshx/`,启动 `./sshweb -p 40009 -d`(无 systemd/cron,手动),配置 `./sshweb-config.enc`。更新流程同本地:
  备份 → `scp -J 172.18.237.219 <新二进制> 172.18.237.201:/home/sshx/sshweb.new` → `mv` 替换 → kill 旧 PID →
  `/home/sshx` 下 `./sshweb -p 40009 -d` → `ss -ltn | grep 40009` + curl 校验。注意该机 Apache 占用 :80,勿动。
  备份约定 `/home/sshx/sshweb.bak.<日期>-<用途>`,回滚即 `mv` 回旧文件 + 重启。
- **当前部署状态(2026-09-18,含「字号设置 + 右键菜单不越界」)**:本地 `:2223` 跑的是**最新态**
  (`sha256 478fef4c8161469ad402faba4b09acb35f05b9c9069bbd0d3536a07ca38b94d6`,**9,510,816 B**,PID **1738189**,
  构建 id `1789719613608`,配置 `sshweb-config.enc` 未动)。部署后逐个 `curl` 回 `build/` 的 **53 个未压缩文件**与本地
  逐字节比对:**53/53 相同**(`.br`/`.gz` 是同源变体,不在比对的 53 里),`/_app/version.json` 的 id 与本地一致。
  上一态(依赖瘦身最终态)是
  `sha256 b59a1c0d291c0f6c3e349f27537fd39de0abd771de131637a74a9dcc551661fa`,**9,506,720 B**,构建 id 与工作区 `build/`
  一致(由 `tmp-buildid-check.mjs` 证明);更早的 `90ad67e9…` 体积完全相同,差别**只有** SvelteKit 的构建 id
  ——即 `npm run build` 的时间戳,见坑 92。
  本次这一版的 `build/` 与旧态的差异由 `tmp-buildid-check.mjs` 记录:**12 个文件真实差异 + 12 个新增 + 129/153 归一化
  后相同**(差异全部落在 `Settings`/`XTerm`/`ContextMenu`/i18n 与共享 CSS 那几块,与改动范围吻合)。
  体积账:审计前 `10,627,424 B` → **−1,116,608 B(−10.51%)**;`build/` `4,069,756 B` → 本次又 +4,096 B(二进制)。
  **回滚链**(都是 `mv` 回旧文件 + 按上面步骤重启):`/home/sshweb/sshweb.old-20260918-fontmenu`
  (9,506,720 B,加字号设置/菜单钳位前)、`sshweb.old-20260918-frontendrebuild`(9,506,720 B,构建 id 变化前那份)、
  `sshweb.old-20260918-xtermaddons`(9,502,624 B,addon 换代前)、`sshweb.old-20260918-deps`(9,592,736 B,依赖瘦身
  第一档前);更早的 `...old-20260917-localsettings`/`...depaudit` 已清理,需要时从 git 历史重建。
  落地探针:未认证时 `POST`/`DELETE /api/proxies/local` 与 `DELETE /api/proxies/{key}` 均 **401**(405 即路由没挂上,
  见坑 86)、`POST /api/host-keys/forget` 带 `{}` → 422(合法 body → 401)、`/api/nope` → 200 落 SPA;
  **`build/` 的 153 个文件逐个 `curl` 回来与本地逐字节比对,153/153 相同**、`/_app/version.json` 的 id 与本地一致、
  启动后日志 0 条 ERROR。
  ⚠️ **201 仍未同步**(`ssh -J 172.18.237.219 172.18.237.201`,`/home/sshx/sshweb` 还是审计前的
  `0ed972425a7f463a30833da4…`/`10,627,424 B`,跑在 `:40009`)——即**两边现在不是同一份前端**,201 缺依赖瘦身与
  addon 换代。同步时按上面「201 部署」那段走(备份约定 `/home/sshx/sshweb.bak.<日期>-<用途>`);它的 Apache :80 未动。


- **未完成的临时脚手架(2026-09-17,仅本机 WSL,验证坑 76/78 的 PATH 差异用)**:`/etc/profile.d/50-postgresql-path.sh`
  与**假的** `/usr/lib/postgresql/15/bin/psql`(67 字节,只 echo 一行)。它们的作用是复现「PATH 由 profile.d 注入」的
  201 环境,好让 `tmp-probe.py` 的非登录/登录差异可见。**留着不要动**,删掉后本机两种入口都会显示 `NO-PSQL`(那是脚手架
  没了,不是回归);确认完再删:`rm /etc/profile.d/50-postgresql-path.sh /usr/lib/postgresql/15/bin/psql`。
- **测试 SSH 服务**:`sshd -p 2222`(用户 sshtest / 密码 test123;root 曾设 test123,仅测试环境)。
- **性能/正确性测试台**(脚本在仓库 `.doc/`,不进版本管理(已在 `.gitignore` 忽略),用完可删):`tmp-bench-dl.py <安装密钥> [端口]
  [ssh端口] [远端路径]` 量单请求整流 / 16 路并发分片 / 首 1MiB 延迟;`tmp-verify-dl.py [端口] [ssh端口] <路径>...
  按 **sha256 逐字节**比对整文件、中间/末尾/跨片 Range 与小文件、空文件;`tmp-delay-proxy.py <监听端口> <目标端口>
  <单向延迟ms>` 造高 RTT(坑 79 用 2224 = 10ms/单向≈20ms RTT、2225 = 50ms/单向≈200ms RTT);`tmp-probe.py <安装密钥>`
  打印四种入口的 shell 矩阵(`argv0`/`ps -p $$ -o args=`/`shopt -q login_shell`/PATH)。判据永远是 **sha256 相等**,
  不是「客户端说成功」;上传的边界用例:8MiB、其后 1KiB、1 字节、正好一片/一片+1/一块+1、空文件(空文件也要发一次
  offset 0 的写,前端就是这么做才建出文件的)。同链路用 `scp`/`sftp put` 做参照基线(修复前 2.9 MiB/s vs scp 23~24
  MiB/s,修复后 21.6 MiB/s)。一次性 Python 脚本用 `websockets`+`cbor2`(venv 建在 `/tmp`,WSL 重启即清)。
- **浏览器 E2E**(Edge/Chrome headless + CDP:`/json/new` 建 target + `Input.dispatchMouseEvent`/
  `Network.webSocketFrame*`;临时实例 `--port 805x --config /tmp/sshweb/test-*.enc`,测后清进程/文件——脚本自带
  清场**只管自己的 sshd,一次性 sshweb 实例要手工杀**)。四个台(主机密钥、四项改进、字体、构建 id),
  分别在 2026-09-17 与 2026-09-18 的最终产物上跑到 **ALL OK**:
  - **主机密钥台**(坑 80):一次性 sshd 用**只含一个 `HostKey /tmp/hk/host_current`** 的独立配置(`sshd.conf`)跑在
    2227,`host_current` 在 `host_a`/`host_b` 之间来回替换 = 模拟「重装系统」。**勿用 `sshd -h`**:那是往默认密钥表
    **追加**,客户端可能协商到别的密钥而看不到替换。`tmp-hostkey-e2e.py <安装密钥> [端口] [ssh端口]` 只打 API(12
    项:记录 A → 换 B 必失败且带两个指纹 → forget 204/重复 404/空 target 400/未认证 401 → 重连成功 → 换回 A 再
    失败);`tmp-hostkey-ui.mjs [端口] [ssh端口] [调试端口]` 把三个 UI 入口全跑一遍(测试连接 / 终端 / SFTP),判据
    是 **`forget` 请求体里的 target 与 204**——target 解析错会变 404,这是唯一能卡住「弹窗看着能用实际没用」的断言
    (UI 那轮 20 项,另存截图 `tmp-hostkey-ui.png`)。
  - **四项改进台**(坑 81-85):`node tmp-four-ui.mjs [端口] [调试端口]` **自带一次性实例**(WSL 里新建配置、独立
    端口、不需要 SSH 服务),覆盖 toast 折行/单击复制/悬停暂停、本机设置弹窗四项与五条持久化路径、新建终端家目录 +
    启动命令 + 不点终端就聚焦、直连代理(⚡/API/仅 `127.0.0.1`/curl 转发 HTTP 200)、终端编码 gbk 对照;另存截图
    `tmp-four-ui.png`。**2026-09-18 在 addon 换代后的最终产物上重跑,38 项 ALL OK**——即换代后的终端在真实浏览器里
    (新开终端 / 焦点 / 起始目录 / 启动命令 / gbk 转码)与换代前一致。
  - **字体探针**(`node tmp-font-probe.mjs [调试端口]`,2026-09-18 新增,8 项 ALL OK):不起 sshweb,只把真实的
    `build/_app` 软链到临时静态目录后驱动 headless Edge,**验的是「字体」这条链**:`@font-face` 真的能命中、
    `document.fonts.load()` 对**不存在的字族**返回**空数组而非 reject**(这是 `XTerm.svelte` 判「字体不可用」的
    唯一依据,见坑 91)、Fira Code VF 的 `i/W/M/./空格/l/@` **步进极差 0.0000px**(等宽成立)、无任何 `.woff` 请求、
    且**只下载了 `inter-latin` 一个 Inter 子集**(`unicode-range` 切分生效的直接证据)。
  - **构建 id 台**(`node tmp-buildid-check.mjs [端口]`,2026-09-18 新增,**部署前**跑):把工作区 `build/` 归一化
    (build id + `__sveltekit_*` 标识符 + 自动推导的改名 chunk 哈希;`.br`/`.gz` 先解压再比)后与**线上那份**逐字节
    比对,回答「这次重建到底改了什么」。退出码 0 = 零功能差异。**需要一个装着旧构建的实例**在跑才能对照 ——
    备份二进制正是干这个的:先 `setsid` 起 **旧的**那个到别的端口(如 2224),再指过去。2026-09-18 用它验证:
    4 个 chunk 改名、153/153 文件相同(详见坑 92)。
  - **字号 + 右键菜单台**(`node tmp-fontmenu-e2e.mjs [端口]`,2026-09-18 新增,自带一次性实例,**31 项 ALL OK**):
    一次跑完两条链。①字号:默认值 = 14(与改前的硬编码一致)→ 写进 store → **`$effect` 真的重跑了**
    (`选项写进了 xterm`:量字元素变成 20px、行高 18.00→25.00px、xterm 自己的模型 123→86 列)→ 客户端确实
    重发了 winsize → **终端里 `stty size` 跟着变**(`41×118 → 29×88`);另有超范围(999)不生效、刷新后仍生效两条。
    ②右键菜单:视口四角右键后整块菜单都在视口内(实测 208×366)、真实鼠标按键确实产生 `contextmenu`、
    与旧硬编码钳位(216/330)的对照、窄窗口(500px)/矮窗口(420px)下仍不越界、宽窗口下子菜单**不**无谓左翻、
    窄窗口下才翻。**判据都在真终端/真浏览器上**(`stty size`、`getBoundingClientRect`),不用 DOM 推算代替。
    该台踩的坑见坑 96(读 `stty size` 的时序 + 环形缓冲的帧计数)。
  四个踩过的坑:①杀进程**勿用 `pkill -f <特征串>`**——`bash -lc` 的命令行里就含该串,会把执行它的 shell 一起杀掉
  (表现为 `exit 15`、后续命令全没跑),改用 pid 文件或 `ss -ltnp` 取 pid;②CDP 侧:服务器面板**默认是关的**(要点
  tab 栏的开关,且「连接」会自动收起它,每个相位前都要确认),`[role=dialog]` **不止主机密钥弹窗**(编辑服务器
  表单也是),必须按标题文本定位到具体那一个;③**往 `cdp.js()` 的模板字符串里写页面内代码时,注释里不能出现反引号**
  ——反引号会**提前结束**那个模板字符串(`tmp-font-probe.mjs` 第一版写 `` `monospace` `` 就炸在
  `SyntaxError: missing ) after argument list`,而报错指向 `cdp.js(` 那一行,看着像调用写错了);④**页面内代码与
  宿主脚本的两种「作用域/求值」坑**:`catch` 是 `try` 的**兄弟作用域**(在 `try` 里 `const` 的东西 `catch` 里看不见,
  失败转储要用到就必须提到外面);`a ??= await f()` 在 `a` 非空时**根本不求值右边**——探测脚本里连写四次右键
  只发出去一次。另:量等宽
  **不要拿通用 `monospace` 家族做基准**(Fira Code 步进 ≈0.6em、Windows 默认等宽 ≈0.5em,不等才正常),
  要比的是**同一字面内各字形的步进是否一致**;且单个空格在 `display:inline-block` 里会**塌成 0 宽**,
  必须 `white-space: pre` 并把字形重复若干次再除。


---

## 七、后续建议 (未完成项)

> 仅保留**仍待办 / 刻意延后 / 保持现状**项。2026-09 已完成的工作归档于 git 历史,细节以提交为准。

**2026-09-18 依赖审计的剩余项**(来源与逐项证据:`.doc/dependency-audit.md`,不入版本管理;编号即该报告 §4 清单的编号。
已做完的 1/2/3/4/6/7/9/10/11/12/13/14 见坑 87-91):

- **【第 5 项,未做】`iconv-lite` 改动态 `import()` + 走原生 `TextDecoder`,下掉 `buffer`**:首屏 gzip **−35 KB**
  (146 KB 移出首屏)+ `buffer` −26 KB。风险低但**有一处必须小心**:「保存为 GBK」那条路径依赖 `encodeBytes` 同步可用,
  改动态 import 后要保证调用点在用之前**已经 await 过**;`lib/encoding.ts` 与 `ui/XTerm.svelte` 都要看一遍。
- **【第 8 项,未做,待用户答复】`xterm-addon-image` 不再无条件加载**(首屏 gzip **−18 KB**):它现在在
  `XTerm.svelte` 里**无条件** `import()`。要动得先确认**是否真的用终端内图片**(Sixel / iTerm2 OSC 1337 图形协议);
  若用,我的建议是**看到引导符再 import**(在字节流里扫 `\x1bP` / `\x1b]1337;File=`),不做成「默认没有」。
- **【第 16 项,未做,需先确认日志过滤需求】`tracing-subscriber` 去掉 `env-filter`**:省 **271 KiB `.text`(7.2%)**,
  这是**单项收益第二大**的一项,代价是失去 `RUST_LOG` 的**按 target 过滤**能力(只剩等级过滤)。用户若不需要
  细粒度过滤,这项值得做。
- **【第 17 项,建议单独排期】** `russh` 0.60.3 → **0.63.3**:审计里**安全收益最大**的一项(8 条公告,4 条命中服务端)。
  代价:`ssh-key` 换官方包、`hazmat-allow-insecure-rsa-keys` 消失(可能影响老服务器)、3 处 API 签名变更、
  `rand` 版本交界要编译验证。**第 18 项(`nix` 0.27/0.31 合一)必须先升 russh 才能做**,同批。
- **【第 19 项,可选,不影响首屏】** 编辑器语言按需裁减(`@lezer/*` 占 gzip 28.6%,但已懒加载)。
- **【第 20 项,可选】** `firacode` → `@fontsource-variable/fira-code`:需先实测**框线字符**(`─│┌┘` 这类 TUI 常用字符)
  在子集化后是否仍首屏可用,不敢直接换。
- **【第 15 项,明确不做】** `pin-project` 换手写 unsafe 投影:只有 2 字段投影 + `PinnedDrop`,手写 unsafe 不划算。
- **【已评估,保持现状】** `Cargo.lock` 里 41 个 crate 有多版本、22 个预发布版:绝大多数由上游锁定
  (典型如 `rand` 0.8 与 0.10,见文末「保持现状」),**唯一能动的一条链就是上面第 17/18 项**。

- **⚠️ 201 与本地已不是同一份构建(待同步)**:本地 `:2223` 是审计瘦身后的
  `b59a1c0d291c0f6c3e349f27537fd39de0abd771de131637a74a9dcc551661fa`(9,506,720 B),201 `/home/sshx/sshweb` 还是
  审计前的 `0ed972425a7f463a30833da4…`(10,627,424 B)。**两份前端不同**,201 缺依赖瘦身与 addon 换代。
  同步按 §六「201 部署」走(备份 → `scp -J` → `mv` → kill 旧 PID → `./sshweb -p 40009 -d` → 探针);
  ⚠️ **不要把重启步骤作用到跳板机或目标 SSH 服务器**,201 的 Apache :80 勿动。
- **addon 换代后仍未覆盖的运行时面**:坑 90 的换代已用两个真实浏览器台验过(见 §六:终端能开、有焦点、起始目录与
  启动命令生效、gbk 转码正确;**字体命中、`document.fonts.load` 对缺失字族返回空数组、Fira Code 等宽步进一致、
  无 `.woff` 请求**)。**刻意没验**的是:①终端**缩放/`fit()`**(容器尺寸变化后行列是否重算)、②**CJK 双宽格对齐**、
  ③**`xterm-addon-image` 的渲染**(与上面第 8 项一起决定)、④**断线重连的回放**(环形缓冲重写)。前两项在
  Windows 侧的 Edge 上量不出可信数字(**浏览器是 Windows 的**,CJK 走的是 Windows 字体回退,不代表用户环境),
  要真验需在目标浏览器/目标字体环境下逐项看。

- **真实 GBK 远端回归验证**:终端编码 (GBK) 只在 UTF-8 的测试 sshd 上验过(坑 9),尚无真实 GBK 远端回归;坑 85 修的
  是「非 UTF-8 输出被静默丢弃」这条**通路**缺陷(本机/远程共用 `Transcode`),不改变「远端确为 GBK 时才该配」的判断。
- **「本机」设置的两处刻意不覆盖(已写进对话框文案)**:①本机**文件浏览器不跟随终端的实时 cwd**(维持现状);
  ②文件管理器视图按 key 缓存(`views.get("local")`),改了家目录**只对之后新开的视图生效**;另外本机 `encoding`
  只作用于**终端**,文件浏览/下载是纯字节流(文本按文件在前端切换编码)——要让文件也按该编码转换是另一条链路,
  需单独确认后再做。
- **上传仍是一块在途(前端未并发)**:服务端已把一块内的写并发下发(坑 77),但前端 `upload.ts` 依旧「发一块、
  等 ack 再发下一块」,吞吐 ≈ `块大小 / RTT`。4MiB 块下已接近同链路 `scp` 水平,再要走高只能让前端同时压
  2~3 块(注意服务端 `WRITE_QUEUE_CAP` 与丢 ack 重发的重复写边界,推进前先确认)。刻意留待需要时再做。
- **流式 zip 仍是一读一等(未并入坑 79 的预取)**:`sftp/zip.rs::archive_remote_stream` 还是顺序
  `file.read(want, buf)`(`ARCHIVE_READ=256KiB`),每个 RTT 只推 256KiB。20ms RTT 下每 2.56MiB 多付一个 RTT
  (≈30% 开销),比单文件下载轻,故本次未动;要改需把 `DownloadReader` 的预取抽出来共用,并给多个条目加有界通道。
- **musl 静态版吞吐低于 gnu 动态版(未深究)**:同代码、同链路、同 4MiB 块,musl 最高 15.7 MiB/s,gnu 最高
  21.6 MiB/s(≈ -27%),但 gnu 波动大(14.7~21.6)、两者都不是严格对照;201 只能用 musl,暂不追(可疑点:
  musl 的 malloc/内存拷贝)。若以后大文件上传仍是瓶颈,从这里查。
- **SOCKS5 隧道的主机密钥不符报不出来(坑 80 未覆盖的第四个入口)**:隧道的 SSH 连接是**懒建连**(`proxy.rs::
  get_connection` 在每条入站连接时才建),`POST /api/proxies` 只登记不连接,于是主机密钥不符既不进开启响应、
  也不进任何连接失败消息,表现为「隧道开了但连不通」。要覆盖需把入站连接的建连错误回灌到隧道状态里(或开启时先
  探测一次),等真有需要再做。
- **201 经跳板链路的修复后实测**:延迟台的 21.6 MiB/s 已随构建部署,但**用户真实链路(浏览器 → 201,SFTP 段还要
  经跳板 172.18.237.219)的实测数仍待确认**,以用户侧上传结果为准。
- **片参数为硬编码常量**:`MAX_PARALLEL_WRITES=16` / `PIECE_BYTES=256KiB`(`sftp/remote.rs`)是按 OpenSSH
  通告的 `max-write-length` 定的,适配特殊服务器(通告值更小/更大)时再考虑改成按协商值计算。
- **流式 zip 边界**:无 zip64(>4GB / >65535 条目报错)、无断点续传(如需再考虑临时文件方案)。
- **大文件编辑器 light 模式「替换」已禁用**:搜索仍可用,按需求保留(非缺陷)。
- **完整 ARIA 键盘化(可选)**:自绘弹窗已控件化、Esc/焦点已就绪,剩余为逐项增强,非必需。
- **浏览器回归(持续)**:自绘弹窗 / 拖放 / 表单控制台无警告 —— 密码框两条 Chromium 提示已于 2026-09-09 修复(见 坑 72),
  随改动持续人工回归。

**已评估但刻意不做 / 延后的重构**(均为行为零变化约束下的取舍,勿轻易重开):

- SOCKS5 ATYP 地址编解码(ssh.rs 出站 vs proxy.rs 入站)三份分支可抽共享函数 —— 触碰线协议、价值有限,保持现状。
- 默认值(macs 空=全选、端口归一化、authMethod/encoding 默认)完全收口到 `connections.ts::serverConfig` ——
  表单侧已保证非空,macs 的「空=全选」在 `serverConfig` 存 `[]` 回读时由 startEdit 特判,现状自洽,重收口无行为收益。

**保持现状(上游/依赖决定,影响小)**

- **rand 双版本(0.8 与 0.10)**:0.8 由本仓库直接代码 + ssh-key 分支(num-bigint-dig) + tungstenite(axum ws)使用;
  0.10 由 russh 系锁定 —— 跨大版本由上游决定,无法合并,保持现状(影响小)。

