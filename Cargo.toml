[workspace]
members = ["crates/sshweb-core", "crates/sshweb-server"]
resolver = "2"

[workspace.package]
version = "0.4.1"
authors = ["fayfoxcat"]
license = "MIT"
description = "A browser-based web SSH terminal and remote file manager."
repository = "https://github.com/fayfoxcat/sshweb"
documentation = "https://github.com/fayfoxcat/sshweb"
keywords = ["ssh", "web", "terminal", "sftp"]

[workspace.dependencies]
anyhow = "1.0.62"
aes-gcm = "0.10.3"
argon2 = "0.5.3"
clap = { version = "4.5.17", features = ["derive", "env"] }
openssh-sftp-client = "0.15"
rand = "0.8.5"
serde = { version = "1.0.188", features = ["derive", "rc"] }
serde_json = "1.0.106"
sshweb-core = { version = "0.4.1", path = "crates/sshweb-core" }
tokio = { version = "1.40.0", features = ["rt-multi-thread", "macros", "net", "io-util", "fs", "time", "signal", "sync"] }
tokio-stream = { version = "0.1.14", features = ["sync"] }
tracing = "0.1.37"
tracing-subscriber = { version = "0.3.17", features = ["env-filter"] }

# Size-optimized release build: maximum density at the cost of compile time.
[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
# Single-process server: no unwinding needed, saves code size + improves perf.
panic = "abort"
